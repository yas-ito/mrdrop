# 手元の設定（置き場所と合言葉が入るので配らない）。退避した .bak も
config.json
config.json.bak-*

# install-windows.ps1 が作るもの
scripts/*.generated.vbs
*.log

node_modules/
.DS_Store
Thumbs.db

# Xcode（すべて ios/project.yml から `xcodegen generate` で作られる。手で直さない）
ios/MrDrop/Info.plist
ios/MrDrop/MrDrop.entitlements
ios/ShareExtension/Info.plist
ios/ShareExtension/ShareExtension.entitlements
ios/**/*.xcodeproj/
ios/**/*.xcworkspace/
ios/**/xcuserdata/
ios/**/DerivedData/

# 配布物を作る場所（build/make-package.js）
_build/
