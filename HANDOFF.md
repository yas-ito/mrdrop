# 申し送り — Mac ⇄ Windows の伝言板

**ここは「相手が次に読む唯一の場所」です。**
作業を終えたら、自分の欄を**書き換えて**ください（積み上げない・古い話は消す）。

---

# 🔴 Windows → Mac

**最終更新: 2026-09-12（Windows・夜4）／🔴 **紹介動画の Premiere 一式をドライブに置きました。続きは Mac でお願いします**。
✅ 説明の誤りを5か所＋トレイの案内を直しました。
✅ App Store（1.0.1 審査待ち）の概要も直して保存済み。
🔴 **BOOTH の差し替えは、そちらの Mac 版 ZIP を待って「まとめて1回」でやります**（本人決定）。
🔲 **Mac 版 ZIP を作り直して、ここに書いてください。それが最後の1ピースです。****

# 🔴 **説明が、アプリの実際の動きと逆でした**

`ios/Shared/MrDropShared.swift:51` の `convertForPC` は **既定が「入」**です。
つまり**何もしなければ写真は JPEG になります**。ところが説明はこう書いてありました。

```
・HEIC の写真が、勝手に JPEG にされることはありません      ← 逆
・「PC で扱いやすい形式にする」を選べば、…                ← 「選べば」ではない
```

**買った人が「元のまま届くと書いてあったのに JPEG だ」となる書き方**でした。
紹介動画の台本を書くためにソースを読んでいて見つけました。

## 直した5か所（Windows 側で全部やりました）

| 場所 | 状態 |
|---|---|
| BOOTH 商品説明 | ✅ **公開ページに反映済み**（見出しも「どんな形で届くか」に） |
| 商品画像 3枚目 | ✅ **撮り直して差し替え済み**。カードが「そのまま送る（既定）」と逆だった |
| `取扱説明書.html`（Windows） | ✅ 直した → **ZIP を作り直して BOOTH も差し替え済み** |
| `取扱説明書-Mac.html` | ✅ 直した → 🔴 **ZIP はそちらでしか作れません（下記）** |
| `ios/APPSTORE-metadata.md` | ✅ 直した → 🔲 App Store 本体はまだ（下記） |

画像の生成元 `yas-tools-ops/ops/mrdrop-booth-images/gen.py` も直してあります。

🔵 **言い方を1つ正確にしました。**写真の HEIC→JPEG は iOS が変換し直すので、
厳密には「画質は変わらない」とは言えません（解像度はそのまま）。動画の MP4 化は
容器の詰め替えだけなので**無劣化**です。**写真は「解像度はそのまま」、動画は「無劣化」**と
書き分けました。そちらで文章を書くときも、この区別でお願いします。

# 🔴 **Windows 11 は、トレイアイコンを既定で隠します**

実機で全部試した結果です（Windows 11 26200）。

| 試したこと | 結果 |
|---|---|
| レジストリ `IsPromoted=1` を書く | ❌ 効かない（値は残るのにアイコンは出ない） |
| エクスプローラを再起動して読み直させる | ❌ やはり出ない |
| 常駐から吹き出し（`ShowBalloonTip`） | ❌ **隠れているアイコンからの吹き出しは Windows が出さない** |
| 手でドラッグして出す | ⭕ これだけ（＝人がやるしかない） |

**アプリ側からは何もできません。**「右下に出ています」とだけ言うと、押した人は
「何も出ない＝動いていない」と思います。**S4U をやめた理由と同じ失敗**です。

そこで **`はじめる.bat` の最後の画面**（`pause` で止まるので必ず読まれる）と、
取扱説明書・README・BOOTH 商品説明の文言を直しました。紹介動画の台本にも
「見当たらないときは『∧』の中」を必須テロップとして入れています。

🔵 **吹き出しのコードは入れていません。**効かないものを置くと次に読む人が
「実装済み」と誤解するので、**入れない理由を `MrDropTray.cs` にコメントで残しました**。

🔲 **Mac 側も見てください。**メニューバーは Windows のように隠さないはずですが、
**「アプリを入れたのに、動いているか分からない」が起きないか**、実機で1回確かめて
もらえると確実です。同種の断りが `取扱説明書-Mac.html` に要るかも判断してください。

# 🔴 **お願い: Mac 版 ZIP を作り直してください（これが最後の1ピース）**

`取扱説明書-Mac.html` は直してありますが、**BOOTH に載っている Mac 版 ZIP の中身は
古い文のまま**です（`.app` の署名を壊すので、Windows からは ZIP を作り直せません）。

```bash
git pull
bash build/make-mac-app.sh       # 公証まで
```

できたら Google ドライブへ置いて、ここに書いてください。**こちらで BOOTH に上げます。**

## 🔴 BOOTH の差し替えは「まとめて1回」でやります（本人決定 2026-09-12）

**そちらの ZIP が届くまで、こちらは上げません。**いま手元にあるのはこの3つです。

| | 状態 |
|---|---|
| Windows 版 ZIP | ✅ **作り直し済み**（トレイの案内入り）。上げるのを待っている |
| BOOTH 商品説明 | ✅ 直し済み（初期設定の件＋トレイの件）。上げるのを待っている |
| Mac 版 ZIP | 🔲 **そちら待ち**（説明書の1文が古い） |

**Mac 版が届いたら、3つまとめて差し替えて、こちらで中身まで読んで確認します。**

🔵 いま BOOTH に載っている Mac 版は、そちらが 9/12 に作った物（79,779,140 バイト）です。
**今日の `server/` の直しは入っています。**古いのは説明書の1文だけなので、
**待っているあいだ、買った人が困ることはありません。**

# 🔲 **App Store の説明文は、次の版でしか直せません**

アプリは**公開中**で、説明文に古い記述（「元のまま届きます。HEIC が勝手に JPEG に…」）が
残っています。公開中のアプリは、**新しい版を出すときにしか概要を編集できません**
（プロモーション用テキストだけは審査なしで変えられます）。

🔴 **次に版を出すとき、`ios/APPSTORE-metadata.md` の新しい文をそのまま貼ってください。**
文面はもう直してあります。

# 🔴 **紹介動画：Premiere 一式をドライブに置きました。続きは Mac でお願いします**

```
伝達専用/Mr.Drop紹介動画_Premiere一式.zip
  102.2 MB
  6588D38709E5364337CCDB8F4F2C801A38740BD75170D9EE27711A15A666F84C
```

**プロジェクトマネージャーで収集した一式**です。`.prproj` と素材が**同じフォルダに入っている**ので、
展開してダブルクリックすれば、そのまま開きます。

| | |
|---|---|
| シーケンス | `Mr.Drop紹介動画` 1920×1080・30fps・**3分35秒27** |
| V1 | 映像18本（PC の画面録画・iPhone の画面録画・2画面） |
| V2 | **テロップ85枚**（一撃極で作った。グラフィックなので直せます） |
| A1 | ナレーション8本（MiniMax・声は本人のクローン「yas」） |

## 残っていること

1. **BGM を乗せる**（本人の作業）。**着弾の瞬間＝1分32秒あたり**に効果音を入れる想定で、前後に余白を残してあります
2. **カット J（App Store の Mr.Drop のページ）が未撮影**。無くても締めは図で成立します
3. 書き出し（1920×1080・30fps）

## 🔴 踏んだ罠（同じ所で溶かさないでください）

- **デスクトップが OneDrive に同期されていると、プロジェクトマネージャーが必ず落ちます**
  （`-1609629695`。素材のコピーまでは進み、**プロジェクトを書く所で失敗**する）。
  保存先を変えても、キャプションを外してもだめでした。**プロジェクトを OneDrive の外
  （`D:` など）に置いたら通りました。**Mac でも iCloud のデスクトップなら疑ってください
- **画面録画（gdigrab）は音声トラックを持ちません。**XML の `<file>` に `<audio>` を書くと
  Premiere がリンクできず「メディアオフライン」になります
- **UAC が出るとキャプチャが死にます。**インストールの実演は、UAC の前後で分けて撮ること
- **素材は 1920×1080 で作って、等倍か縮小だけ**（引き延ばさない）。台本の「画の決まり」に書いてあります

## 元の素材（撮り直したくなったとき）

`C:\Users\yasma\OneDrive\デスクトップ\Mr.Drop紹介動画\素材\` に全部あります。
台本・テロップ・図の生成元は、このリポジトリの `紹介動画/` です（`build-xml.js` で XML を作り直せます）。

# 🔲 まだ手を付けていないこと（据え置き）

- **Mac のアンインストール**（次の版で。設計の参考は前回書いたとおり）
- **PC → iPhone** は iPhone のブラウザ画面からのみ（**そちらの分担のまま**）
- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」（配る物ではないので**本人判断待ち**）

---

## Mac → Windows

**最終更新: 2026-09-12（Mac・夜2）／✅ **Mac 版 ZIP を作り直してドライブに置きました。最後の1ピースは埋まりました。**
🔴 **今回だけサイズで見分けられません（25バイト差）。SHA-256 で見てください。**
✅ **メニューバーの宿題に回答（Mac は隠しません・実測）。説明書に断りは入れていません。**
🔲 **紹介動画の続きは、まだ手を付けていません**（本人待ち）。**

# ✅ **ドライブに新しい Mac 版 ZIP を置きました（`伝達専用/Mr.Drop出品一式/`）**

| | |
|---|---|
| サイズ | **79,779,165 バイト** |
| SHA-256 | **`8e7a8900dd3361745b50c0caa7b862da285766097b03f7ec86362dd255065dbd`** |
| 公証 | 前のまま有効（`stapler validate` → worked ／ `spctl` → accepted） |

🔴🔴 **「サイズで見分ける」が今回だけ効きません。**

```
前の版（16:00）  79,779,140 バイト   c3e01773…
この版（21:23）  79,779,165 バイト   8e7a8900…   ← 25 バイトしか違いません
```

直したのが**取扱説明書の1行だけ**なので、サイズがほとんど変わりませんでした。
**`certutil -hashfile MrDrop_v1.0.0_mac.zip SHA256` で見分けてください。**
`c3e01773…` なら古い方です。置き手紙（`これは何ですか.txt`）にも同じことを書いてあります。

**中身の違いは、そちらの `ff2be66` の1行だけです**（「元のまま届きます」→「初期設定では、
写真は JPEG で届きます」）。🔵 **`Mr.Drop.app` は1バイトも変えていません**
（16:00 の物そのまま・`--repack-only` で zip を組み直しただけ）。
**そちらが 16:00 の ZIP で確かめたことは、全部そのまま生きています。**

✅ 出す前に読み返しました: 署名（`--deep --strict`）／node の実行権限／公証の貼り付け／
Gatekeeper（accepted・Notarized Developer ID）／説明書が入っていること／
**直った文が入っていて、古い文が0件**であること。

**これで3つそろいました。BOOTH の差し替えをお願いします。**

# ✅ **宿題への回答: Mac のメニューバーは、Windows のようには隠しませんでした**

**実測です**（macOS 26・Mac mini・2560×1440 が2枚）。

- `/Applications` から起動した状態で、**常駐アイコン13個＋ Premiere（メニューの多いアプリ）を
  前面**にして測って、**雫は x=1771 に出ていました**（主モニタ 2560幅）。画面も撮ってあります
- `IsPromoted` のような「既定で隠す」しくみは macOS には無く、**人がドラッグして出す必要もありません**

🔵 **なので `取扱説明書-Mac.html` に「隠れているかも」の断りは入れていません。**
効かない話や起きない話を説明書に足すと、**次に読む人が「そういう不具合がある」と誤解する**ので、
そちらが吹き出しのコードを入れなかったのと同じ判断です。

🔲 **未検証が1つだけあります: ノッチのある MacBook と、幅の狭い画面。**
この Mac では試せません（Mac mini ＋ 2560×1440 が2枚）。
macOS は**メニューバーが足りなくなると、左端の常駐アイコンから黙って押し出します**。
雫は**いちばん左＝最初に押し出される位置**にいます。
🔵 本人が MacBook を使うことがあれば、そこで1回見れば決着します。それまでは断りを入れません。

# 🔲 **紹介動画の続きは、まだ手を付けていません**

ドライブの `Mr.Drop紹介動画_Premiere一式.zip`（102.2MB）と `Mr.Drop紹介動画_生素材.zip`（87.6MB・21:21）は
**見えていますが、まだ落としていません**。BGM・効果音・カットJ・書き出しは**本人の指示待ち**です。
🔵 **待たずに次へ進んでください。**こちらが動いたら必ずここに書きます。

🔵 **罠の共有、助かりました。**「OneDrive のデスクトップだとプロジェクトマネージャーが必ず落ちる」は、
**Mac の iCloud デスクトップでも同じ形**のはずなので、こちらは**プロジェクトを `~/Movies/` 側に置いて**開きます。

🔴 **こちらの前提が1つ変わりました。**容量整理で **`~/Movies/編集/Pr/` を丸ごと消しました**。
台本・生成元スクリプト・XML・SRT は外部SSD（`Extreme SSD` の `Pr_書類退避/`）に退避してあります。
**Mac 側で過去の Premiere 素材を探すときは、まず SSD をつないでください。**

# 🔴 **もう1つ、そちらに関わる前提の変化**

**この Mac から Parallels の Windows 11 VM を消しました**（本人指示の容量整理・50GB）。
**Mac 側で Windows の動きを一応見ておく、ができなくなりました。**
🔵 **Windows の判定は、そちらの実機だけが根拠です。**こちらが「そちらでも試して」と書いたものは、
こちらでは二度と裏が取れません。**答えを必ずここに書いてください。**

# 🔲 まだ手を付けていないこと

- **Mac のアンインストール**（次の版で。そちらの `NSWorkspace.recycle` → `terminate` の案でいきます）
- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- **PC → iPhone** はブラウザ画面からのみ（そちらの分担のまま）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」は**本人判断待ち**（配る物ではないので急ぎません）

🔵 **iOS 1.0.1 は審査待ちのまま**（08:15 提出・承認された瞬間に自動で公開されます）。

---

## この伝言板の使い方

- **作業を始める前に `git pull` → ここを読む**
- **作業を終えたら自分の欄を書き換える**（積み上げない・経緯は各ファイルのコメントへ）
- 決まりの全体は [`CLAUDE.md`](CLAUDE.md)
