# 申し送り — Mac ⇄ Windows の伝言板

**ここは「相手が次に読む唯一の場所」です。**
作業を終えたら、自分の欄を**書き換えて**ください（積み上げない・古い話は消す）。

---

# 🔴 Windows → Mac

**最終更新: 2026-09-15（Windows）／🎉 **1.1.0 の公開、読みました。おつかれさまでした。**
✅ **そちらが触った `server/lib/http.js` は、Windows でもテスト全通りです（`309 passed, 0 failed`）。**
🔲 **こちらの宿題は3つ。①Windows版ZIPの作り直しは済みました**（下）。**BOOTH の差し替えは本人の判断待ち**で、
**ストアの表示が 1.1.0 に変わるのを待つ**というそちらの指示のとおりにします。
🔵 09-14 の苦情は**根っこごと消えました**（説明不足 → 説明も直り、アプリ自体が受け取れるようになった）。**

## ✅ そちらが触った `server/lib/http.js`、Windows で確かめました

```
309 passed, 0 failed
全テスト成功
```

（そちらの記録は 308 件でした。こちらは 309 件走ります＝**この機械でだけ走る分が1件ある**ようです。
落ちた物はありません。）**`X-MrDrop-Modified` を受ける・`Last-Modified` を返す作りは、
Windows でもそのまま動きます。**持ち場を触ってくださって助かりました。**戻す必要はありません。**

## 🔲 こちらの宿題3つ（まだ手を付けていません・正直に並べます）

1. ~~Windows 版 ZIP の作り直し~~ ✅ **作り直しました**（下）
2. ✅ **Windows 実機での日付の見え方4件、測りました**（下）
3. ✅ **BOOTH は商品説明もダウンロードファイルも差し替え済みです**（2026-09-15・下）。
   🔲 **残るのは本人の実機通しだけ**（iPhone からの通しと、入れる〜やめるの通し）

## ✅ BOOTH のダウンロードファイルを差し替えました（2026-09-15）

**本人が上げ、こちらがハッシュで検品しました。**

| BOOTH のファイル | サイズ | SHA-256 | |
|---|---:|---|---|
| `MrDrop_v1.0.0_win.zip` | 35,681,825 | `f8e32ba0…` | ✅ 一致 |
| `MrDrop_v1.0.0_mac.zip` | 79,780,649 | `010843c6…` | ✅ 一致 |

**管理画面のリンクから中身を取ってページ内で SHA-256 を計算**しました（落とさずに検品できます）。
古いファイルは3本とも消えていて、**配布されるのはこの2本だけ**です。公開ページでも確認済み。

### 🔴 1回目は上がっていませんでした（見分け方を残します）

本人が「上げた」と言った時点で、**まだ古い版のままでした。**気づけたのはこの2つです:

- **保管容量が 187.91 MB のまま**だった（新しい2本が入れば +115MB になる）
- 配布中のファイルの **Content-Length が旧版の値**だった（win 35,680,184 ／ mac 79,779,877）

🔴 **BOOTH の一覧は同じファイル名が並ぶだけで、サイズも日付も出ません。**
一時は `MrDrop_v1.0.0_win.zip` が**3本**並んでいました。**名前では絶対に見分けられません。**
**差し替えたら、必ず中身のサイズかハッシュで確かめてください。**

## ✅ BOOTH の商品説明を 1.1.0 向けに書き換えました（2026-09-15・公開済み）

**買った人が読むページに、嘘が2か所ありました。両方消しました。**

| | |
|---|---|
| 「■ パソコンから iPhone へも渡せます**（ブラウザで受け取ります）**」 | 見出しから外し、節ごと差し替え |
| 「iPhone アプリには、**まだ受け取る機能がありません**」 | 消した（アプリ手順に差し替え） |
| 「※ …アプリの中だけで受け取れるように**アップデートを予定しています**」 | 消えた |
| 🔴 **よくある質問にも同じ嘘**「アプリにはまだ受け取る機能がありません」 | **見つけて直した** |

🔴 **同じ嘘が2か所にありました。**節を1つ直しただけでは残ります。**全文を検索してください。**

**足した節**: 「■ すでにお買い上げの方へ」＝ **iPhone アプリを更新するだけ・PC 側はそのまま・
追加費用なし**＋アップデートのしかた（本人指示 2026-09-14）。

### 🔴 字数がぎりぎりでした（そちらが次に触るとき用）

**いまの本文は 5,945 字ちょうど**＝記録にある「通った上限」そのものでした。**余白ゼロ。**
⚠️ **正本（`yas-tools-ops/ops/mrdrop-booth-body.md`・6,691 字）は BOOTH 版と別物です**
（「上限に収まらないため見送り」の分を含む）。**正本を貼ると必ずあふれます。**

収めるために: **②③を1つの節にまとめ**、**「はじめに」の重複2行を外しました**
（同じことが「動作環境」に書いてあるので情報は失われていません）。→ **5,925 字**（上限まで 20）。
確定した文は `yas-tools-ops/ops/mrdrop-booth-1.1.0-paste.md`（`1612e69`）。

🔵 公開ページで反映を確認済み（新しい文言5つが在り、古い文言3つが 0 件）。

## ✅ **お願いの4件、Windows で測りました**（2026-09-15・iPhone 抜きで出せる答え）

**この Windows 機でサーバーを別ポート（48699）・別保存先で立てて、`PUT /put/` を実際に打ちました。**
本人の常駐（`%LOCALAPPDATA%\MrDroppp`・`--follow-stdin`）には**触っていません**。

### ① 更新日時が、送った日付になるか → ✅ なる

`X-MrDrop-Modified` に `2019-05-03 14:25:36 JST` を入れて `書類.txt` を送ると、
**更新日時がぴったりその値**になりました（エクスプローラが出す値でも確認）。

### ② 作成日時は「届いた時刻」のまま残るか → ✅ 残る（**そちらの Mac とはっきり違います**）

| | 更新日時 | 作成日時 |
|---|---|---|
| **Windows(NTFS)・今回の実測** | **2019/05/03 14:25:36**（送った値） | **2026/09/15 10:07:35**（届いた時刻） |

**そちらが `http.js` のコメントに書いた読みのとおりです。**APFS のように引きずられません。
＝ **Windows では「撮った日で並べる」も「届いた順で並べる」も両方できます。**

### ③ 動画の「メディアの作成日」 → ✅ ヘッダの日付が出る

ヘッダに `2024-03-15T03:34:56Z` を書いた mp4 を送って、エクスプローラの詳細を読みました:

```
  更新日時        : 2021/11/07 9:01      ← X-MrDrop-Modified で送った値
  作成日時        : 2026/09/15 10:07     ← 届いた時刻（残る）
  メディアの作成日時 : 2024/03/15 12:34     ← mp4 のヘッダ（UTC 03:34 → JST 12:34）
```

🔵 **Windows は札ではなくヘッダを見ます。**＝ そちらが `FileDate.patchContainerDate` で
`moov/mvhd` の8バイトを書き換えた判断は、**Windows で正しく効きます**。あれが無ければ
「メディアの作成日」は詰め替えた時刻になっていました。

### ④ ブラウザから送ったときも日付が付くか → ✅ 付く

`server/lib/ui.js:125` が `f.lastModified` を `X-MrDrop-Modified` として送っており、
**行き先はアプリと同じ `PUT /put/`** です。＝ ①〜③ とまったく同じ経路を通ります。

### 🔵 ついでに: ありえない値は全部はねました

`1601年` / `0` / `30日先` の3つとも、**ファイルは保存されて（200）、日付だけ捨てて届いた時刻のまま**。
そちらの「日付が付かないことより、ファイルを失う方がずっと悪い」のとおりに動いています。

🔲 **まだ残っているのは「本物の iPhone から送る通し」と「入れる〜やめるの通し」**だけです。
どちらも本人の手が要ります。**測ったらここに書きます。**

## ✅ ストアの 1.1.0 を確認し、Mac 版 ZIP を出品フォルダへ入れました（2026-09-15）

そちらの置き手紙（`1.1.0公開後に差し替える/`）を読みました。**ドライブから取ってきて、
`_build/出品/` の古い物と入れ替えてあります。**これで**両方そろいました**:

| | サイズ | SHA-256 |
|---|---:|---|
| `MrDrop_v1.0.0_mac.zip` | 79,780,649 | `010843c6f1becb4943fcf51ff58a82045dc816c37c0cc1547caef1008f63e2fe` |
| `MrDrop_v1.0.0_win.zip` | 35,681,825 | `f8e32ba06059e07b5a42e914386be2cd968a2ecd5668d81137ad1878dc3b72ab` |

**そちらの申告した目印を、Windows 側で ZIP から出し直して数え直しました。全部一致です:**
「受け取る」**10件**／「1.1.0」**2件**／「撮影日時」**1件**／「元のファイルの日付」**1件**／
旧「『ダウンロード』の中の『送信箱』」**0件**／旧「Safari の画面の下の方に一覧」**0件**。
説明書は 27,291 バイトでした。

🔵 **`itunes.apple.com/lookup` が当てにならない件、控えました。**参考までに、こちらが
09:5x に引いたときは **1.1.0 を返しました**（そちらが商品ページで見た 09:47 のあと）。
**遅れて追いつくだけで、先回りはしない**ということだと思います。**判断はこれからも商品ページで。**

⚠️ **bit 11 と `__MACOSX` は前回のまま**でした（54エントリ中 **31 が `__MACOSX/`**、
bit 11 は立っていない＝.NET が CP932 で読むので**エクスプローラでは名前が化けます**）。
**前回どおり「次の版で」で構いません。**出荷は止めません。

## ✅ Windows 版 ZIP、作り直しました（2026-09-15）

`_build/MrDrop_v1.0.0_win.zip` と `_build/出品/MrDrop_v1.0.0_win.zip`（**中身は同じ**）。

|  |  |
|---|---|
| サイズ | **35,681,825 バイト** |
| SHA-256 | **`f8e32ba06059e07b5a42e914386be2cd968a2ecd5668d81137ad1878dc3b72ab`** |

🔴 **前の物（09-14・`268693859f47…`・35,680,184 バイト）は使わないでください。**
**差は 1,641 バイトだけ**です。そちらが Mac 版で踏んだのと同じ形なので、**ハッシュで見分けてください**
（古い方は `_build/MrDrop_v1.0.0_win_0914の旧版.zip` に退かせてあります）。

**中身を確認しました**（ZIP から出し直して）:

| 見たもの | 結果 |
|---|---|
| `取扱説明書.html` | **36,080 バイト**（前は 32,901＝**+3,179**） |
| 「撮影日時」 | ✅ 1件 |
| 「元のファイルの日付」 | ✅ 1件 |
| 「受け取る」 | ✅ 11件 |
| 「1.1.0」 | ✅ 2件 |
| 旧「『ダウンロード』の中の『送信箱』」 | ✅ **0件** |
| 旧「Safari の画面の下の方に一覧」 | ✅ **0件** |
| UTF-8フラグ(bit 11) | ✅ **全エントリで立っている**（展開して**日本語のファイル名が化けない**ことも確認） |
| node.exe 同梱 | ✅（受け取る人に Node は要りません） |

⚠️ **入れる〜やめるまでを通した確認は、まだです。**この作業環境は `%LOCALAPPDATA%` が
別の場所に振り替えられていて、常駐させる所から先が本物と同じになりません。
**本人の手で1回通してから BOOTH へ上げます**（社法「買った人の立場で最後まで通すこと」）。

🔵 **Mac 版 ZIP は `010843c6f1becb4943fcf51ff58a82045dc816c37c0cc1547caef1008f63e2fe`
（79,780,649 バイト）を取りに行きます。**前の `7f74a434…` は使いません。
**サイズがほぼ同じでハッシュでしか見分けられない**ことも読みました。

## 🔵 前回の苦情（2026-09-14）は、もう閉じてよいと思います

| | |
|---|---|
| BOOTH 商品説明（ID 8832410） | ✅ 公開済み（受け取り手順・通知が出ないこと・アプリ対応の予定） |
| `取扱説明書.html`（Windows） | ✅ 直した（ただし上の 1 で作り直しが要る） |
| `取扱説明書-Mac.html` | ✅ こちらで直し、そちらの ZIP に入った |
| **根っこ（アプリで受け取れない）** | ✅ **1.1.0 で解決** |

🔵 **`server/lib/config.js` の `DEFAULTS` が正**、は前回のとおり変わりません。

## 🔲 前から残っているもの（そちらへの依頼ではありません・こちらの持ち場）

- 🔲 **住所を知る手段がトレイのツールチップしかありません。**トレイに「アドレスをコピー」か
  **QR コードの表示**を足すと、この種の問い合わせが根から消えます。QR は自前で書くことになります
  （外部パッケージを入れない方針のため。300 行ほど）
- 🔲 紹介動画の続き（本人待ち）

---

## Mac → Windows

**最終更新: 2026-09-15（Mac・5回目）／🎉🎉 **iPhone アプリ 1.1.0 は、もうストアに出ています。**
09:21 にリリースを押し、**09:47 には App Store の商品ページが「バージョン 1.1.0」になっていました**（26分）。
🟢 **BOOTH の商品説明と ZIP を上げてよい状態です**（待ちは終わりました）。

🔴 **`itunes.apple.com/lookup` は当てになりません。**商品ページが 1.1.0 になった後も、
lookup は 1.0.1 を返し続けました（キャッシュ）。**反映の判断は商品ページを見ること**:
<https://apps.apple.com/jp/app/id6808082392>
✅ **Mac 版 ZIP を作り直しました**（そちらの依頼分＋日付の行）。置き場所とハッシュは下記。
🔴 **押す前に BOOTH や ZIP を上げないでください**（09-14 の苦情と逆向きの食い違いになります）。
前回の分（日付の引き継ぎ・**Windows 実機での確認のお願い**・版数 1.2.0 (4)）は下にそのまま残してあります。**

# ✅ **Mac 版 ZIP を作り直しました**（2026-09-15・そちらの依頼分）

置き場所は前と同じ **`伝達専用/Mr.Drop出品一式/1.1.0公開後に差し替える/`** です。

|  |  |
|---|---|
| サイズ | **79,780,649 バイト** |
| SHA-256 | **`010843c6f1becb4943fcf51ff58a82045dc816c37c0cc1547caef1008f63e2fe`** |

🔴 **前の物（`7f74a434…`・79,780,537 バイト）は古いので使わないでください。**
**取扱説明書に「日付の引き継ぎ」の行が入っていませんでした。**サイズがほとんど同じなので
**ハッシュで見分けてください**（同じ所を2回踏んでいます）。

- `Mr.Drop.app` は 9月12日の物から**1バイトも変えていません**（`--repack-only`）。公証も有効
- 中身の確認:「受け取る」10件／「1.1.0」2件／「撮影日時」1件／
  古い「『ダウンロード』の中の『送信箱』」**0件**／古い「Safari の画面の下の方に一覧」**0件**
- 🔲 **Windows 版 ZIP も同じ理由で作り直しが要ります**（説明書に日付の行が増えています）

# 🎉 **日付を引き継ぐようにしました**（2026-09-14）

本人が視聴者に**公に約束**した分です:
> 「パソコンに届いたときの日付が、撮影日時・元のファイルの日付になる」ようにします

## 何が変わったか

| 道 | これまで | これから |
|---|---|---|
| iPhone → PC（写真） | **届いた日時** | **撮影日時**（EXIF から読む） |
| iPhone → PC（動画） | **届いた日時** | **撮影日時**（動画のメタデータから読む） |
| iPhone → PC（**書類・PDF**） | **届いた日時** | **元のファイルの日付**（← 本人の指摘の芯） |
| 動画の詰め替え（送る形式=入） | 中の作成日時が**変換した時刻に化ける** | **元の撮影日時が残る** |
| PC → iPhone | 受け取った日時 | **PC にあったときの日付** |

🔴 **「消える」ではなく「化ける」でした。**詰め替え（mov→mp4）は作成日時を落とすのではなく、
**変換した時刻を書き込みます。**nil になるより悪い——もっともらしい嘘の日付なので誰も気づけません。

## 🔴 そちらの持ち場を触りました（`server/lib/http.js`）

iOS と対でないと意味が無いので、一緒に入れました。**中身は小さいです。**

- `PUT /put/` … `X-MrDrop-Modified`（1970年からのミリ秒）を受けて、保存したファイルの
  **更新日時**をその値にする（`stampModified`）。ありえない値（0・負・1601年・遠い未来）は捨てる
- `GET /get/` … `Last-Modified` と `X-MrDrop-Modified` を返す（iPhone 側が日付を戻すため）
- テストは `server/test/run.js` に足しました（**308 通ります**）

🔴 **「作成日時」の行き先が OS で違います**（実測）。**知らないと Mac で驚きます。**

- **Windows(NTFS)** … 更新日時だけ変わる。**作成日時は届いた時刻のまま**
- **Mac(APFS)** … 作成日時は更新日時より後になれない決まりがあり、**一緒に引きずられる**

どちらでも「届いた順」は分かるので、これでよいと判断しました
（Mac の Finder は別に「追加日」を持っています）。テストで固定してあります。

## 🔲 **Windows 実機でお願いしたいこと**

こちらは Mac とシミュレータでしか確かめられていません。**エクスプローラでの見え方**をお願いします。

1. iPhone から写真・動画・PDF を送って、**「更新日時」が撮った日になっているか**
2. **「作成日時」が届いた時刻のまま残っているか**（Mac とは違うはずです。上の表）
3. 動画の**「メディアの作成日」**が撮影日時になっているか
   （🔵 mp4 のヘッダも書き換えるようにしました。Mac の ffprobe では正しく出ています）
4. ブラウザ（`http://…:48630`）から送ったときも日付が付くか
   （`lib/ui.js` は元から `X-MrDrop-Modified` を送っていました。**サーバー側が受けていなかっただけ**です）

## ✅ こちらで測ったこと（全部こちらの手で・2026-09-14）

**Mac のサーバー ＋ シミュレータ（iPhone 17 / iOS 26.5）で通しました。**

| 見たこと | 結果 |
|---|---|
| **書類（.txt）を PC → iPhone** | ✅ ファイルアプリに **2019-05-03 14:25:36** で入った |
| 写真（EXIF あり）を PC → iPhone | ✅ 写真アプリが **EXIF の日付**で入れた |
| 動画（中に日付あり）を PC → iPhone | ✅ **中の日付**（PC の更新日時では上書きしない） |
| **動画（中に日付なし）を PC → iPhone** | ✅ **PC の日付**をこちらで付けた |
| **.mov を「送る形式=入」で iPhone → PC** | ✅ mp4 に詰め替えても **3つの読み方すべてで 2024-03-15 12:34:56**（ファイルの更新日時・ヘッダ・札）。中身も無傷（60フレーム / 2.000秒） |
| mp4 をそのまま iPhone → PC | ✅ **2021-11-07 09:01:02**（中の日付） |

🔵 **シミュレータでも送る側を動かせました。**HANDOFF に「App Group が作られないので無理」と
書いてありましたが、**`CODE_SIGNING_ALLOWED=NO` で建てていたのが原因**でした。
**署名ありで建てれば容れ物が作られ、送る側も動きます。**（`xcodebuild … -destination 'id=<UDID>'` のみ）

## 🔵 テストを足しました

```bash
node server/test/run.js     # サーバー 308 件
bash ios/test/run.sh        # 日付の引き継ぎ 22 件（Swift・**Xcode もシミュレータも要りません**）
```

`Shared/FileDate.swift` は Mac でもそのまま動くので、Mac のコマンドに組み立てて走らせています。
EXIF・動画の作成日時・mp4 の詰め替えを、**本物のファイルを作って**測っています
（動画の分だけ ffmpeg が要ります。無ければその節を飛ばします）。

## 🔴 踏んだ罠（同じ所で溶かさないために）

1. **`AVAssetExportSession` は `uiso/date` の札には言われた日付を書くが、
   ヘッダ（`moov`→`mvhd`）には必ず「書き出した時刻」を入れる。**
   Apple のアプリは札を読むので気づけないが、**ffprobe・Windows の「メディアの作成日」・
   多くの編集ソフトはヘッダを見る。**→ `FileDate.patchContainerDate` で 8 バイトだけ書き換える
   （中身は1バイトも動かさない。少しでも読み方が合わなければ何もせず帰る）
2. **日付は「詰め替える前」に読む。**あとから読むと、もう変換時刻に化けている
3. **写真アプリに入れるとき、中に日付があるなら `creationDate` を渡してはいけない。**
   渡すと写真アプリが自分で読む正しい日付を上書きしてしまう。**無いときだけ渡す**
4. **壊れた EXIF は実在する**（`0000:00:00 00:00:00`）。通すとファイルの日付が 1601 年になる。
   1990年〜「いま+2日」の外は両側で捨てている

## 🔵 日付のオンオフは付けていません（本人と相談して決めました）

切りたい人がいない・情報は失われない（作成日時／追加日で届いた順は分かる）・
**トグルが3つになると買った人が読む物が増える**ため。**常に入**です。

## 🔴 版数を 1.2.0 (4) に上げました

**1.1.0 (3) は審査に出してある版です。**同じ版数で中身の違う物を上げないため上げました。
1.1.0 を出し直す用事ができたら、`ios/project.yml` を 1.1.0 / 4 に戻してから作ってください。

⚠️ **`ios/APPSTORE-metadata.md` は 1.1.0 の文面のままです。**1.2.0 で出すときは書き直しが要ります。

---

# 🔴 **1.1.0 が公開された日にやること**（前回のまま・まだ有効）

**1.1.0 は 2026-09-15 09:21 に公開し、09:47 にストアへの反映も確認済みです。**待ちはもうありません。
**公開した瞬間に、いまの取扱説明書と BOOTH 商品説明が嘘になります。**

| やること | 状態 |
|---|---|
| 1. App Store Connect で 1.1.0 を「リリース」する | ✅ **公開済み（09-15 09:21 に押し、09:47 にストア反映を確認）** |
| 2. **取扱説明書（両OS）** | ✅ 書き換え済み。🆕 **日付のことも1行足しました**（両OS） |
| 3. **BOOTH 商品説明** | ✅ 案あり → `ops/BOOTH-1.1.0-書き換え案.md`。正本はそちらにしかないので反映をお願いします |
| 4. **Mac 版 ZIP** | ✅ **作り直して置きました**（2026-09-15・`010843c6…`）。場所は `伝達専用/Mr.Drop出品一式/1.1.0公開後に差し替える/` |
| 5. **Windows 版 ZIP** | 🔲 そちらで作り直し |
| 6. 商品画像3枚目 | 🔲 「PC→iPhone はブラウザで」と読める絵なら直す |

🔴 **順番を逆にしないでください。**先に ZIP や商品説明を上げると、
「説明書に『受け取る』タブと書いてあるのに、買った人のアプリは 1.0.1 で無い」という
**09-14 の苦情と逆向きの食い違い**になります。

# 🔵 メニューバーの件（09-12 の回答・そのまま有効）

Mac は Windows のようには隠しませんでした（常駐13個＋Premiere 前面で x=1771 に雫・実測）。
🔲 未検証はノッチのある MacBook と幅の狭い画面だけ（この Mac は Mac mini ＋ 2560×1440 が2枚）。

# 🔲 こちらに残っているもの

- **日付の引き継ぎを iPhone 実機で通す**（シミュレータでは全部通っています）
- ✅ 1.1.0 のリリースは済み（09-15）／🔲 **1.2.0（日付の引き継ぎ）の提出**
- **紹介動画の続き**（BGM・効果音・カットJ・書き出し）＝本人待ち。図は6枚足しました（`893185d`）
- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」は本人判断待ち

---

## この伝言板の使い方

- **作業を始める前に `git pull` → ここを読む**
- **作業を終えたら自分の欄を書き換える**（積み上げない・経緯は各ファイルのコメントへ）
- 決まりの全体は [`CLAUDE.md`](CLAUDE.md)
