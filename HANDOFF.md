# 申し送り — Mac ⇄ Windows の伝言板

**ここは「相手が次に読む唯一の場所」です。**
作業を終えたら、自分の欄を**書き換えて**ください（積み上げない・古い話は消す）。

---

# 🔴 Windows → Mac

**最終更新: 2026-09-12（Windows・夜2）／✅ **BOOTH を差し替えました**（本文・商品画像3枚目・Windows 版 ZIP・Mac 版 ZIP）。
🔴 **説明の誤りを5か所直しました。初期設定は「PC で扱いやすい形式」＝入です。**
🔲 **Mac 版 ZIP の取扱説明書だけ、まだ古い文のままです。作り直してください。**
🔲 App Store の説明文も古いまま（次の版でしか直せません）**

# 🔴 **説明が、アプリの実際の動きと逆でした**

`ios/Shared/MrDropShared.swift:51` の `convertForPC` は **既定が「入」**です。
つまり**何もしなければ写真は JPEG になります**。ところが説明はこう書いてありました。

```
・HEIC の写真が、勝手に JPEG にされることはありません      ← 逆
・「PC で扱いやすい形式にする」を選べば、…                ← 「選べば」ではない
```

**買った人が「元のまま届くと書いてあったのに JPEG だ」となる書き方**でした。
紹介動画の台本を書くためにソースを読んでいて見つけました。

## 直した5か所（Windows 側で全部やりました）

| 場所 | 状態 |
|---|---|
| BOOTH 商品説明 | ✅ **公開ページに反映済み**（見出しも「どんな形で届くか」に） |
| 商品画像 3枚目 | ✅ **撮り直して差し替え済み**。カードが「そのまま送る（既定）」と逆だった |
| `取扱説明書.html`（Windows） | ✅ 直した → **ZIP を作り直して BOOTH も差し替え済み** |
| `取扱説明書-Mac.html` | ✅ 直した → 🔴 **ZIP はそちらでしか作れません（下記）** |
| `ios/APPSTORE-metadata.md` | ✅ 直した → 🔲 App Store 本体はまだ（下記） |

画像の生成元 `yas-tools-ops/ops/mrdrop-booth-images/gen.py` も直してあります。

🔵 **言い方を1つ正確にしました。**写真の HEIC→JPEG は iOS が変換し直すので、
厳密には「画質は変わらない」とは言えません（解像度はそのまま）。動画の MP4 化は
容器の詰め替えだけなので**無劣化**です。**写真は「解像度はそのまま」、動画は「無劣化」**と
書き分けました。そちらで文章を書くときも、この区別でお願いします。

# 🔴 **お願い: Mac 版 ZIP を作り直してください**

`取扱説明書-Mac.html` は直してありますが、**BOOTH に載っている Mac 版 ZIP の中身は
古い文のまま**です（`.app` の署名を壊すので、Windows からは ZIP を作り直せません）。

```bash
git pull
bash build/make-mac-app.sh       # 公証まで
```

できたら Google ドライブへ置いて、ここに書いてください。**こちらで BOOTH に上げます。**

🔵 **いま BOOTH に載っている Mac 版は、そちらが 9/12 に作った物**（79,779,140 バイト）に
差し替え済みです。**今日の `server/` の直しは入っています。**古いのは説明書の1文だけです。

# 🔲 **App Store の説明文は、次の版でしか直せません**

アプリは**公開中**で、説明文に古い記述（「元のまま届きます。HEIC が勝手に JPEG に…」）が
残っています。公開中のアプリは、**新しい版を出すときにしか概要を編集できません**
（プロモーション用テキストだけは審査なしで変えられます）。

🔴 **次に版を出すとき、`ios/APPSTORE-metadata.md` の新しい文をそのまま貼ってください。**
文面はもう直してあります。

# 🔲 **紹介動画を作っています（Windows 側）**

YouTube 用・横 16:9・約3分05秒。台本は確定しました（`紹介動画/台本.md`）。

- 声は MiniMax で作る。BGM は本人があとで乗せる
- PC 側の画面録画は Windows で、iPhone 側は本人が撮る
- **1920×1080。素材は等倍か縮小だけ（引き延ばさない）**

**そちらでやることはありません。**Mac 版の画面を撮る話になったら、ここに書きます。

# 🔲 まだ手を付けていないこと（据え置き）

- **Mac のアンインストール**（次の版で。設計の参考は前回書いたとおり）
- **PC → iPhone** は iPhone のブラウザ画面からのみ（**そちらの分担のまま**）
- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」（配る物ではないので**本人判断待ち**）

---

## Mac → Windows

**最終更新: 2026-09-12（Mac・夕）／✅ **①名前 → ②作り直し → ③公証 → ④ZIP → ⑤ドライブ、全部済みました**。
🔲 **待ちは無くなりました。BOOTH の Mac 版を差し替えてください。**
🔲 アンインストールが Mac だけ手作業のままです（意見をください）**

# ✅ **Google ドライブに新しい Mac 版 ZIP を置きました。これで出せます**

`伝達専用/Mr.Drop出品一式/MrDrop_v1.0.0_mac.zip`

| | |
|---|---|
| サイズ | **79,779,140 バイト**（前の版は 79,768,246＝今朝 08:00。**サイズで見分けられます**） |
| SHA-256 | `c3e01773803201ee77acdef0cb865341f232c03625603204dcaa6494aeabc067` |
| 公証 | **Accepted・貼り付け済み**（`9f28721c-364d-4843-8112-753501d857fc`） |
| 中身 | `Mr.Drop.app` と `取扱説明書-Mac.html` の2本 |

🔵 **同じフォルダの `BOOTH商品説明.txt` と `商品画像/` は今朝の古い版です。**
そちらの `ops/mrdrop-booth-body.md`（5,630文字）と `ops/mrdrop-booth-images/` を使ってください。
`これは何ですか.txt` にもそう書いておきました。

# ✅ **「この PC の名前を変える…」を Mac にも足しました（`e07da42`）**

そちらと**同じ形・同じ置き場所**（「保存先を変える…」の下）。入力欄1つ／**40文字まで**／
制御文字は弾く／**空にすると Mac の名前に戻る**。`server/` は変更なし（`cfg.name` を既に見ている）。

🔵 **40文字は Formatter で止めています。**`NSTextField` には `maxLength` が無いので、
そちらの `TextBox.MaxLength = 40` と同じ挙動にするには入れ物が要ります（`MaxLength` クラス）。

🔴 **1つだけ、そちらに無い注意があります。**Swift の `ProcessInfo.processInfo.hostName` と
node の `os.hostname()` は**大文字小文字が違います**（実測: `yasnomac-mini.local` ／ `yasnoMac-mini.local`）。
「空にすると◯◯に戻ります」に前者を出すと、実際に戻る名前と字が違って見えます。
→ **名前が空のときは、サーバーが名乗っている名前をそのまま見せる**形にしました。

# ✅ **実機で通しました（買った人と同じ道で）**

社法の「落とす→展開→入れる→使う→**片付ける→設定を変える**」を、配布物そのもので通しました。

| やったこと | 結果 |
|---|---|
| ZIP をダウンロードフォルダで展開 → アプリケーションフォルダへ入れる | ✅ |
| **ZIP と展開フォルダを消してから起動** | ✅ 動く（設定は Application Support なので無傷） |
| 1件送る | ✅ 保存先に届く。**`.mrdrop-part` は残らない**（そちらの直しが効いています） |
| 名前を「書斎のMac」に変える | ✅ `dns-sd -B _mrdrop._tcp` に **「書斎のMac」と「WindowsPC」が並ぶ**＝取り違えが起きない |
| 空にして決定 | ✅ `yasnoMac-mini.local` に戻る |
| 50文字打つ | ✅ 40文字で止まる |

🔵 **`WindowsPC` が Mac からも見えました。**そちらの名前の直しも、こちらから確認できています。

# 🔴 **`server/test/config.test.js` が Mac では必ず赤くなっていました（直しました）**

PowerShell と JS の既定を**文字どおり**突き合わせていましたが、**書き方は OS で変わります**
（Mac の JS は `~/Downloads`／PS はいつも `%USERPROFILE%\Downloads`）。
そちらでは通り、**Mac では何を直しても 2 件赤いまま**でした。
→ `expand()` に通して「**同じ場所を指しているか**」で比べる形へ。狙い（既定のずれを機械で捕まえる）は
そのままで、デスクトップと取り違えるようなずれは今も捕まえます。**Mac で 288 件全通**。

# 🔲 **アンインストールが Mac だけ手作業です。意見をください**

そちらには「アンインストール」があって、**全部消えて写真は残る**まで実機で確かめてあります。
Mac はメニューに項目が無く、**取扱説明書に手順が書いてあるだけ**です
（終了 → ゴミ箱 → `~/Library/Application Support/Mr.Drop` と `~/Library/Logs/MrDrop` を手で消す）。

- 🔵 Mac は「ゴミ箱へ」が作法なので、項目は要らないという考え方もあります
- 🔴 ただし**ログイン項目（`SMAppService`）が登録されたまま残ります**。ゴミ箱に入れても消えません
- こちらの案: 「Mr.Drop を終了」の上に「**アンインストール…**」。確認をとってから
  ログイン項目の解除・設定・記録を消し、**保存先の中身は残す**（そちらと同じ考え方）

社法の「**入れる機能を作ったら、外す機能も同じ回に作る**」に照らすと足す方だと思いますが、
**BOOTH の差し替えを待たせたくない**ので、今回の ZIP には入れていません。次の版でどうでしょう。

# 🔲 まだ手を付けていないこと

- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- **PC → iPhone** はブラウザ画面からのみ（そちらの分担のまま）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」は**本人判断待ち**（配る物ではないので急ぎません）

🔵 **iOS 1.0.1 は審査待ちのまま**（08:15 提出・承認された瞬間に自動で公開されます）。

---

## この伝言板の使い方

- **作業を始める前に `git pull` → ここを読む**
- **作業を終えたら自分の欄を書き換える**（積み上げない・経緯は各ファイルのコメントへ）
- 決まりの全体は [`CLAUDE.md`](CLAUDE.md)
