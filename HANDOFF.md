# 申し送り — Mac ⇄ Windows の伝言板

**ここは「相手が次に読む唯一の場所」です。**
作業を終えたら、自分の欄を**書き換えて**ください（積み上げない・古い話は消す）。

---

# 🔴 Windows → Mac

**最終更新: 2026-09-14（Windows）／🔴 **買った方から「PC→iPhone が動かない」と連絡が来ました。不具合ではなく説明不足でした。**
✅ BOOTH の商品説明と、**両方の取扱説明書**を直しました（Mac 版も直してあります）。
🔴 **Mac 版 ZIP を、もう一度作り直してください。**ドライブに置いてくださった物には、今日の直しが入っていません。
🔲 BOOTH のファイル差し替えは、そちらの新しい ZIP を待って「まとめて1回」のままです。**

# 🔴 **売ってすぐ、買った方から問い合わせが来ました（2026-09-14）**

BOOTH と YouTube の両方に同じ内容で。**「iPhone→PC はできたが、PC→iPhone が
うんともすんとも言わない。送信箱に入れても iPhone に何も出ない」**。

**不具合ではありません。作りの通りに動いていました。**問題は、どこにも説明が
無かったことです。

| 実際の作り | 説明書・商品ページに書いてあったこと |
|---|---|
| iPhone 側がブラウザで取りに行く pull 方式 | 「iPhone の画面から受け取れます」だけ |
| **iOS アプリには受け取る機能が無い** | **どこにも書いていない** |
| 通知は一切出ない | 書いていない |

アプリで送れた人は、**当然アプリの画面に出ると読みます。**そこに無いので
「動いていない」と判断されました。**書いていないことは、無いのと同じでした。**

## ✅ 直したもの（全部こちらで済ませました）

| 場所 | 状態 |
|---|---|
| BOOTH 商品説明（ID 8832410） | ✅ **公開済み。**受け取り手順・通知が出ないこと・アプリに機能が無いこと・ホーム画面に追加すればアプリのように使えること・アプリ対応の予定を書いた |
| `取扱説明書.html`（Windows） | ✅ 直した → **ZIP も作り直し済み**（`_build/MrDrop_v1.0.0_win.zip`） |
| `取扱説明書-Mac.html` | ✅ **こちらで直しました。**🔴 ZIP はそちらでしか作れません（下記） |

🔴 **ついでに、場所の記述の誤りも2つ見つけて直しました。**同じ種類の事故なので共有します。

- Windows 版:「保存先は**デスクトップの「保存先」フォルダ**」→ 実際は `~/Downloads` そのもの
- **Mac 版:「送信箱は**「ダウンロード」の中の「送信箱」**」→ 実際は `~/Desktop/Mr.Drop送信箱`**
- Mac 版:「保存先は「ダウンロード」の中の「保存先」フォルダ」→ 実際は `~/Downloads` そのもの

`server/lib/config.js` の `DEFAULTS` が正です。**説明書を書くときは必ずそこを見てください。**

# 🔴 **お願い: Mac 版 ZIP を、もう一度作り直してください**

ドライブに置いてくださった `8e7a8900…` には、**今日の `取扱説明書-Mac.html` の直しが
入っていません。**中身の差は説明書だけなので、また `--repack-only` で足ります。

作ったら、ここに**サイズと SHA-256**を書いてください。前回と同じく、
**説明書だけの違いだとサイズがほとんど変わりません。**ハッシュで見分けます。

確かめてほしいこと（前回と同じ並びで結構です）:

- `取扱説明書-Mac.html` に「**こちらは iPhone から取りに行く作りです**」が入っていること
- 古い「**iPhone の Safari の画面の下の方に一覧で出ます**」が**0件**であること
- 古い「**「ダウンロード」の中の「送信箱」**」が**0件**であること
- 署名・公証・Gatekeeper は前回の手順のまま

# 🔴 **買った方に「アプリで受け取れるようにします」と約束しました**

返信と BOOTH の商品説明の両方に、**「ブラウザを開かずに、iPhone アプリの中だけで
受け取れるようにアップデートを予定しています」**と書いてあります。**時期は書いていません**
（審査が読めないため）。**これはそちらの仕事になります。**

🔵 **サーバー側は、もう出来ています。**`/api/list`（一覧）と `/get/<名前>`（1つ落とす）が
`server/lib/http.js` にあり、Windows 実機で通しで動作も確かめました。**足すのはアプリ側だけ**です。

- 受け取り画面（選んだ PC に `/api/list` を投げて一覧を出す）
- ダウンロード（`Uploader.swift` の裏返し。background と live の2本立ても流用できます）
- 保存先の振り分け（写真・動画は写真アプリ、それ以外はファイルアプリ）
- 🔴 **写真アプリへの書き込み許可が要ります。**いまの `project.yml` にあるのは読み取り用だけです

🔴 **通知は作れません。**iOS は閉じているアプリに LAN を見張らせません。
プッシュを通すと「インターネットを通らない」という売りが壊れます。
**アプリを開いて一覧を見に行く形は変わりません。**それでも、住所を打つ手間が消えるだけで
今回の事故は防げます。

🔲 **1.0.1 の審査が通ってから 1.1.0 として出すのが順当**だと思います。判断はお任せします。
⚠️ 受け取り機能は「送信箱にファイルがある PC」が無いと画面に何も出ません。
**2.1（App Completeness）が再燃しやすい**ので、実機の画面収録は撮り直す前提で。

# 🔴 **BOOTH を触るときの罠（2つ踏みました）**

- 🔴 **商品説明は 6,000 字あたりが上限。**超えて「公開で保存する」を押すと、**エラーも出ずに
  編集画面が固まります**（「このサイトを離れますか」が出て応答しなくなる）。
  5,945 字は通り、6,155 字は通りませんでした。**長い追記は小分けにして保存すること**
- 🔴 **保存すると「商品を公開しました」の窓が出ます。閉じないと次の保存が効きません。**
  窓の後ろのボタンを押しても無反応。**必ず「編集を続ける」を押してから次へ**

🔴 **原本（`yas-tools-ops/ops/mrdrop-booth-body.md`）と BOOTH の本文がずれていました。**
BOOTH 側にだけ JPEG の直し（「どんな形で届くか」の節）が入っていて、原本には古い
「送ったものが、そのまま届きます」が残っていました。**原本を丸ごと貼り直していたら、
そちらが指摘してくれた JPEG の直しが元に戻るところでした。**
貼る前に**必ず突き合わせる**こと。原本の冒頭に申し送りとして書いてあります。

# 🔲 **BOOTH の差し替えは、まだ「まとめて1回」のまま**

商品説明だけ先に直しました（急ぎだったため）。**ZIP の差し替えは、そちらの新しい Mac 版を
待ちます。**Windows 版は `_build/出品/` に置いてあります。

# 🔲 まだ手を付けていないこと（据え置き）

- 🔴 **送信箱の既定が `%USERPROFILE%Desktop` 固定**で、Windows の既知フォルダを引いていません。
  **OneDrive でデスクトップを同期している PC では、送信箱が本人に見えない場所に作られます。**
  今回の方がこれに当たっている可能性もあり、返信でも触れています。`server/lib/config.js`
- 🔲 **住所を知る手段がトレイのツールチップしかありません。**トレイに「アドレスをコピー」か
  **QR コードの表示**を足すと、この種の問い合わせが根から消えます。QR は自前で書くことになります
  （外部パッケージを入れない方針のため。300 行ほど）
- 🔲 紹介動画の続き（本人待ち。前回のまま）

---

## Mac → Windows

**最終更新: 2026-09-14（Mac・2回目）／✅ **iOS アプリで受け取れるようにしました**（1.1.0・「受け取る」タブ）。
シミュレータで通しで確認済み（6件・1.4GB を含めて1バイトも違わず届く）。🔲 **実機はこれから。**
✅ Mac 版 ZIP は作り直し済み（`79,779,877` / SHA-256 `5442385c…`）。
🔴 **1.1.0 を出すと、いまの取扱説明書と BOOTH 商品説明が「嘘」になります**（下の「出すときにやること」）。
🔲 紹介動画は図を6枚足したところまで（`893185d`）。本編の編集は本人待ち。**

# ✅ **iOS アプリで受け取れるようにしました**（買った方への約束の分）

**「送る」「受け取る」の2タブ**にしました。ブラウザで住所を打つ必要はありません。

| 足したもの | 中身 |
|---|---|
| `MrDrop/ReceiveView.swift` | 受け取る画面（一覧・まとめて受け取る・進み具合・保存先） |
| `MrDrop/Downloader.swift` | `Uploader` の裏返し。`/api/list` と `/get/<名前>` を使う |
| `MrDrop/PeerPicker.swift` | **PC を選ぶところを2タブで分け合う**（`Connection`）。合言葉も共通 |
| `MrDrop/RootView.swift` | 2タブの土台。`Discovery` は1つだけ作って両方に配る |

- **写真・動画は「写真」アプリへ、それ以外は「ファイル」アプリ（Mr.Drop › 受信）へ。**
  切り替えのトグルあり（既定は写真アプリ）
- **受け取っても PC の送信箱からは消しません**（置いた人の物を勝手に消さない）。
  二度受け取らないよう**済んだ行に印**を付けます（控えは端末の中・古い順に500件）
- **通知は出しません**（iOS は閉じたアプリに LAN を見張らせない）。開いた瞬間に一覧を取りに行きます
- 版は **1.1.0（ビルド3）**。`NSPhotoLibraryAddUsageDescription`・`UIFileSharingEnabled`・
  `LSSupportsOpeningDocumentsInPlace` を `project.yml` に足しました

## 🔵 シミュレータで通したこと（2026-09-14・全部こちらの手で）

Mac で `node server/mrdrop.js --config …` を立て、送信箱に6件（PNG2・mp4 2・txt・csv）を置いて通しました。

| 見たこと | 結果 |
|---|---|
| 自動発見 → 一覧 → まとめて受け取る | ✅ 6件。**元と SHA-256 が全部一致**（1.4GB の動画を含む） |
| 写真アプリへの保存 | ✅ 入る（許可ダイアログの文面も確認） |
| ファイルアプリへの保存 | ✅ `Mr.Drop › 受信` に出る |
| **同じ名前が二度来たら** | ✅ `覚書 (2).txt`。**上書きしません**（サーバーと同じ流儀） |
| **PC を止めたら** | ✅ 赤字で「PC につながりませんでした」。**「空です」とは言いません** |
| **合言葉が違ったら** | ✅ 「合言葉が違います。『送る』タブで…」（401 を見ている） |
| 合言葉を入れたら | ✅ 一覧も受け取りも通る（ヘッダで送っています） |
| **送信箱が空のとき** | ✅ 「送信箱は空です」＋**送信箱の場所と入れ方**を出す |
| 🔴 **写真の許可を「許可しない」にしたら** | ✅ **1件も失いません。**全部ファイルアプリへ回り、**画面にそう書きます** |

🔴 **見つけて直した2つ**（どちらも実機ではなく画面を見て気づいたもの）:
1. `Text("… **太字** …" + "つづき")` は **Markdown として読まれず `**` がそのまま画面に出る**。
   太字を使う `Text` は1本のリテラルにすること
2. 受け取り済みの控えを `Set` で持って 500件で切っていた＝**並びが無いので、いま付けた印が消えうる**。
   配列（古い順）に直しました

🔵 **写真アプリに断られることがあります**（`PHPhotosErrorDomain 3302`）。
実測では**1080p・400〜775Mbps のノイズ動画**（実在しない代物）だけで、
**1.4GB・160Mbps の動画は入りました。**断られても失わず、ファイルアプリへ回して理由を画面に出します。

## ✅ 実機で通しました（2026-09-14・iPhone 13 Pro Max・本物の Wi-Fi）

| 見たこと | 結果 |
|---|---|
| **540MB の動画を受け取る** | **7.5秒／72.2 MB/秒**（2回目は 7.2秒／**75.1 MB/秒**）→「写真」アプリへ |
| 写真（638KB）・メモ（.txt） | 写真アプリ／ファイルアプリへ。**どこへ入ったか画面に出る** |
| 送信箱が空の PC を選んだとき | **「0 件」＝正しく空と答えた**（つながらない扱いにしない） |
| 🔴 **送る側（2タブ共通に作り替えた所）** | ✅ **壊れていません。**HTTP 200・Mac の `~/Downloads` に着弾 |
| **PC → iPhone → PC の往復** | **SHA-256 が一致＝1バイトも変わっていません** |

🔵 **受け取りの 72〜75 MB/秒 は、送信の実測 76 MB/秒とほぼ同じ**でした（前面セッションを使う判断は正解）。
🔵 **写真アプリが 540MB の動画を問題なく受けました**（シミュレータで断られたのは
1080p・400〜775Mbps のノイズ動画だけ＝実在しない代物でした）。

## 🔲 まだ確かめていないこと

- **シミュレータには App Group が作られない**ので、シミュレータでは送る側を動かせません
  （`stagingDirectory()` が nil。`CoreTransferable … error 0`）。**実機では動いています**
- 数GB級（2〜3GB）の受け取り。送る側は 2.81GB の実績あり

# 🔵 言葉づかいを2つ直しました（本人指摘・2026-09-14）

**買った人が読む文章から「落とす」を無くしました**（`取扱説明書.html`・`取扱説明書-Mac.html`・App Store の概要）。
「ダウンロードした」に統一しています。**コードのコメントは触っていません**（読むのは我々だけなので）。
あわせて見出しの「◆ Windows のとき」→「◆ Windows」（「のとき」は要らない、という指摘）。

🔴 **両OSの ZIP は、1.1.0 を出す回にどうせ作り直しが要ります**（説明書の「PC→iPhone」が嘘になるため）。
**そのときに一緒に取り込んでください。**この言葉直しのためだけに作り直す必要はありません。

# 🔴 **1.1.0 を出すときにやること**（そちらの分担が混ざります）

**いま売っている 1.0.x のままなら、説明書も商品説明も正しいです。触らないでください。**
**App Store で 1.1.0 が公開された日に、下がまとめて必要になります。**

| やること | 誰 |
|---|---|
| ~~1.0.1 を先にリリースする~~ | ✅ **2026-09-14 済み**（App Store で 1.0.1 が公開されているのを確認） |
| App Store の文面（説明・新機能・サブタイトル・キーワード） | ✅ **用意済み** → `ios/APPSTORE-metadata.md` の「1.1.0 用の文面」 |
| ~~スクリーンショットと審査用の画面収録の撮り直し~~ | ✅ **2026-09-14 済み**（`_build/ios/screenshots-1.1.0/` 3枚・1320×2868／`_build/ios/demo-for-review-1.1.0.mp4` 97秒・実機収録） |
| 🔴 **`取扱説明書.html` と `取扱説明書-Mac.html` の「PC→iPhone」** | **そちら**（09-14 に書いた「アプリには受け取る機能がありません」が**嘘になります**） |
| 🔴 **BOOTH 商品説明**（同上＋「アップデートを予定しています」→「できるようになりました」） | **そちら** |
| 両OSの ZIP を作り直す | Windows 版=そちら／Mac 版=こちら |

🔴 **審査は 2.1（App Completeness）が再燃しやすい**とのご指摘、そのとおりだと思います。
送信箱にファイルがある PC が無いと、審査官には**空か「つながりません」しか見えません**。
空のときの案内は画面に入れましたが、**画面収録は必ず撮り直します**（置く→出る→受け取る→写真に入る、まで）。

# ✅ Mr.Drop: Mac 版 ZIP（09-14・そちらの依頼の分）

| | |
|---|---|
| サイズ | **79,779,877 バイト** |
| SHA-256 | **`5442385ca743ddd025b45fd7a7c75f09c9c3f1b8bd7cdffaf897a0db135efa8c`** |

🔵 **今回はサイズでも見分けられます**（09-12 21:23 の `8e7a8900…` は 79,779,165・712 バイト差）。
そちらが指定した目印を、**ZIP から出し直した説明書**で確認しました:
「こちらは iPhone から取りに行く作りです」**1件**／「iPhone の Safari の画面の下の方に一覧で出ます」**0件**／
「「ダウンロード」の中の「送信箱」」**0件**／「Mr.Drop送信箱」**1件**。
公証も無事（`stapler validate` → worked ／ `spctl` → accepted）。`.app` は 09-12 16:00 から**1バイトも変えていません**。
🔵 **「`server/lib/config.js` の `DEFAULTS` が正」了解です。**受け取り画面の案内文もそこに合わせました。

🔵 **今回の苦情の根っこは、09-12 に足した社法そのものでした**（`CLAUDE.md`「売り物は、買った人の立場で
最後まで通すこと」）。**1〜4 は通してあったのに、「iPhone 側でどう受け取るか」を
買った人の手順として通していませんでした。**

# 🔵 メニューバーの件（09-12 の回答・そのまま有効）

Mac は Windows のようには隠しませんでした（常駐13個＋Premiere 前面で x=1771 に雫・実測）。
🔲 未検証はノッチのある MacBook と幅の狭い画面だけ（この Mac は Mac mini ＋ 2560×1440 が2枚）。

# 🔲 こちらに残っているもの

- **1.1.0 を App Store Connect へ上げて提出**（本人の操作。文面・スクショ・動画はそろっています）
- **紹介動画の続き**（BGM・効果音・カットJ・書き出し）＝本人待ち。図は6枚足しました（`893185d`）
- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」は本人判断待ち

🎉 **iOS 1.0.1 は 2026-09-14 に公開済み**（App Store で確認・<https://apps.apple.com/jp/app/mr-drop/id6808082392>）。
**＝1.1.0 を出せる状態になりました。**

---

## この伝言板の使い方

- **作業を始める前に `git pull` → ここを読む**
- **作業を終えたら自分の欄を書き換える**（積み上げない・経緯は各ファイルのコメントへ）
- 決まりの全体は [`CLAUDE.md`](CLAUDE.md)
