# 申し送り — Mac ⇄ Windows の伝言板

**ここは「相手が次に読む唯一の場所」です。**
作業を終えたら、自分の欄を**書き換えて**ください（積み上げない・古い話は消す）。

---

## 🔴 Windows → Mac

**最終更新: 2026-09-12（Windows）／🔴🔴 **いますぐ v1.0.1 を出してください。入れた人が詰まっています。**
＋ **PC 側も大きく変えました（受信箱→保存先・既定はダウンロードフォルダ）。Mac 版の作り直しが要ります。**
発売そのものは済んでいます（App Store 05:47 / BOOTH 05:51）。**先頭から読んでください。**

# 🔴🔴🔴 **いますぐお願い：v1.0.1 を出してください。入れた人が詰まっています**

**本人指示（2026-09-12・発売当日）。「最優先で v1.0.1 を出してほしい」。**

> ダウンロードした人が、どうやれば Mr.Drop を使えるかが一番大事。
> これじゃ、ダウンロードされても何をすればいいのかさっぱり分からない。アプリを出している意味がない

## 何が起きているか

`ios/MrDrop/ContentView.swift:173` の「見つからないときは」の案内:

> ・受け取る側のパソコンで **Mr.Drop（PC 版）** が動いている必要があります

**「要る」と書いてあるのに、どこで手に入るのかが書かれていません。**
アプリの中に外部リンクは1つもなく（Swift 全部確認済み。出てくる URL は接続先の PC だけ）、
App Store の説明文も同じところで終わっています。

🔴 **無料で入れて、何もできず、どうすればいいかも分からない。**
**レビューがゼロの新作にとって ★1 が一番こわい。**いま毎日これが起き続けています。

## 🔲 直すこと（1行で足ります）

```
・受け取る側のパソコンで Mr.Drop（PC 版）が動いている必要があります
  PC 版は yas-tools.booth.pm で手に入ります        ← これを足す
```

**できれば、もう1つ。**いまは PC が見つからないまま数秒待たないと案内が出ません
（`noPeerHint`）。**初回起動で1枚**「これは送る側です。パソコン側にも要ります」を出せると、
迷う人がさらに減ります。**ただし、1行の方が先です。急いでください。**

🔵 **リンクボタンにせず、文字で書く**のが安全だと考えています（判断はお任せします）。

## 🔴 決まりが変わりました（本人決定 2026-09-12）

**「購入への導線」は今後いっさい考えません。**
考えるのは **「ダウンロードした人が、どうやったら Mr.Drop を使えるか」** だけ。

- ❌ アプリの中に「**買う**」ボタンやリンクを置く
- ✅ **「PC 側が要る」「どこで手に入る」は必ず書く**（書かないと詰まるので必須）

**売るための工夫ではなく、使えないまま放置しないための案内です。**
2026-09-03 の「アプリの中に『PC 版を買う』導線は置かない」も、この形に書き換えました
（`ios/APPSTORE.md` / `ContentView.swift:169` / `ops/mrdrop-booth-listing.md`・`885f335`）。

## 🔵 審査に出している間、1.0.0 はそのまま配信され続けます

**アプリが店から消えることはありません。**新しい版を提出しても、
**承認されて「リリース」を押すまで、いまの 1.0.0 が配信され続けます。**

⚠️ **つまり、審査に出している間も「入れた人が詰まる」は続きます。**
前回は **9/10 提出 → 9/12 までに承認**でしたが、**その前に Guideline 5.2.5 で1度落ちています。**
**落ちるとさらに数日**です。だから急いでほしい、という話です。

🔲 **新しい版なら、いま編集できない欄も全部直せます。**（公開中の版で編集できるのは
**プロモーション用テキスト・著作権・App Review 情報の3つだけ**でした。実機で確認）
**ついでにこの2つも直してください:**

- **概要（説明文）の「■ 必要なもの」**に入手先を足す
- **マーケティングURL** に `https://yas-tools.booth.pm/items/8832410`
  （App Store のページに「デベロッパWebサイト」として並びます）

## ✅ こちらで先に打った手（これしかできませんでした）

App Store の**プロモーション用テキスト**（**審査不要で直せる唯一の枠**）を、
売り文句から**「先に何が要るか」の案内**に差し替え済み:

> 【先にご確認ください】これは「送る側」のアプリです。受け取るパソコン（Windows / Mac）側にも
> Mr.Drop を入れて動かしておく必要があります。両方が同じ Wi-Fi につながっていれば、
> 写真も動画も撮ったままの画質で直接送れます。

**「入れる前」には気づけます。「入れたあと」に困っている人には届きません。**
🔵 `README.md` も「2つそろって、はじめて動きます」に書き直しました（`0f178c0`）。

---

# 🔴🔴 **PC 側も大きく変えました。Mac 版の作り直しをお願いします**

**本人指摘（2026-09-12・発売直後）。全部 pull 済みで動いています。**

> Mac では受信箱というフォルダを作らず、直接ファイルが指定のフォルダに置かれる。
> 受信箱という表現はおかしいので直すべき
> → 最初から受信箱を作らないように。受信箱という表現をやめる。
> 　 初期設定を Mac・Win 両方ともダウンロードフォルダに

## ① 「受信箱」をやめて「保存先」に（`f30754b`）

**リポジトリ全体で「受信箱」は 0 件**です。`mac/main.swift` のメニューと
`取扱説明書-Mac.html` も**こちらで直してあります**（言葉がずれると困るため）。

- メニュー … 「受信箱を開く」→「**保存先を開く**」／「受信箱を変える…」→「**保存先を変える…**」
- `install-mac.sh` の案内文も同じ

## ② 既定を**両OSともダウンロードフォルダそのもの**に（`f30754b`）

```
保存先  ~/Downloads              ← 専用のフォルダを作らない
送信箱  ~/Downloads/Mr.Drop送信箱
```

🔴 **`mac/main.swift:299` の既定も直してあります**（`Downloads/受信箱` → `Downloads`）。
🔴 **送信箱だけは専用フォルダのまま**にしました。ここの中身は**同じ Wi-Fi から一覧できる**ので、
ダウンロードフォルダそのものにすると**置いてある物が全部見えてしまう**ためです。
テストに「送信箱と保存先が同じ場所でない」を足してあります。**違う考えなら言ってください。**

## ③ Windows 版に「ダブルクリックでできる操作」を足した（`b3e57b2`）

**Mac にはメニューがあるのに Windows は config.json を手で書くしかなかった**ので、揃えました。

| | |
|---|---|
| `保存先を変える.bat` | フォルダ選択が開く → 書く → 常駐なら入れ直す |
| `保存先を開く.bat` | Mac の「保存先を開く」と同じ |
| `いつでも使えるようにする.bat` | 1回押すと常駐（黒い画面が消える）。昇格も面倒を見る |

🔴 **実機で踏んだ罠**: `Stop-ScheduledTask` はプロセスをすぐ殺しません。待ち時間で逃げると
古い方がポートを握ったまま新しい方が立ち上がり、**「48630 番はすでに使われています」で両方止まります**
（実際に止めました）。**時間ではなくポートが空いたことを確かめてから**起動するようにしてあります。

## 🔲 お願い：**Mac 版 ZIP を作り直してください**

`server/` は両OS共通なので pull すれば直っていますが、**`Mr.Drop.app` の中身は作り直しが要ります。**
`mac/main.swift` も変わっているので、**ビルドし直してください**（`--repack-only` では足りません）。

🔵 **急ぎではありません。本人判断で「まだ売れていないので、差し替えはそのまま待つ」**となりました。
**Windows 版も新しい ZIP を手元に置いたまま、BOOTH には上げていません。**
**Mac 版ができたら、両方まとめて差し替えます。**

⚠️ **ただし、1本でも売れる前に差し替えること。**売れたあとに黙って中身を変えると、
その人だけ古い物を持ったままになります。**版は 1.0.0 のまま**でいきます（まだ誰も持っていないため）。

## 🔵 店の文面と画像も直してあります

- 商品説明（`yas-tools-ops` `e1f48ae`）… 「受信箱」ゼロ・4,805文字
- 商品画像5枚（同上）… 2枚目の画面例を新しい既定に。ターミナルとブラウザ画面を**白**に（`dbbdac9`）
- 🔴 Chrome の headless は **OS のダークモードに従います**。白で撮るには
  `--blink-settings=preferredColorScheme=1` が要ります（最初これで黒いまま撮れました）

---

# 🎉 **発売しました**

| | |
|---|---|
| **iPhone アプリ** | <https://apps.apple.com/jp/app/id6808082392>（無料・1.0.0 (1)・05:47 公開） |
| **BOOTH（PC側）** | <https://yas-tools.booth.pm/items/8832410>（**¥500**・商品ID 8832410・05:51 公開） |

🔴 **順番は「App Store のリリース → BOOTH 公開」でやりました。**商品説明が「App Store で無料」と
書き、末尾に実URLを貼っているので、逆にすると買った人が 404 に飛びます。
🔵 **リリースを押してから Apple 側の反映まで約4分**でした（404 → 200）。次も待つこと。

# 🔴 **戻さないでほしいもの（3つ）**

**そちらの手順書・README と食い違っているので、知らないと元に戻します。**

| 直した所 | そちらの記述 | いまどうなっているか |
|---|---|---|
| `gen.py` の `.term` の**色** | README「**どのテーマでも黒のまま**（実物の画面がそうなので）」 | 🔴 **白にしました**（本人指示）。理由は CSS にコメントで残してあります |
| `gen.py` の `.term` の**フォント** | README「Noto Sans JP を入れれば Mac とほぼ同じ見た目」 | `"SF Mono","Menlo"` は**どちらも Mac 専用**でした。`"Cascadia Mono","Consolas"` を足しています |
| 商品画像の**作り直し手順** | README「`python3` と Chrome があれば動きます」 | 🔴 **Windows に Python が入っていませんでした。**入れて解決（3.13）。**手順書に1行足してください** |

🔵 **フォントを足すまで、Windows で作り直すと別物になっていました**（実測）:
バックスラッシュが `¥` で描かれて `C:¥Users¥…` になり、`192. 168. 10. 105` のように字間が開く。
**SF Mono を先頭に残したので、そちらの見た目は変わりません。**

# 🔴 **Mac 版 ZIP のファイル名に UTF-8 の印（bit 11）が立っていません**

差し替えてくださった `MrDrop_v1.0.0_mac.zip` を数えました。**全 54 エントリで 0 件**です。
`ditto` で固めているためで、**Windows で展開すると `取扱説明書-Mac.html` が
`蜿匁桶隱ｬ譏取嶌-Mac.html` に化けます**（実際に化けました）。

🔵 **Mac で展開する分には化けません**（macOS は印が無くても UTF-8 として読む）。買うのは Mac の人なので
**今回はこのまま出しました**。ただ、このリポジトリが `build/make-package.js` でわざわざ避けている罠と
同じものです。🔲 次に作り直すときの扱いは、そちらで決めてください。

# 📌 決まったこと

- **価格 ¥500**（発売記念価格・期限は決めない）。この日のうちに ¥980 →「一週間無料」→
  「一週間だけ ¥500」と動き、最後にここへ落ち着きました。**期限を書かないので、
  「元値・次の価格・期限を書かない」という決めごとはそのまま有効です**
- **取扱説明書と商品説明を「アプリ本命・ブラウザは逃げ道」に統一**（`55567de`）。
  🔴 **両OS共通の `server/mrdrop.js` の起動画面も変えています**。pull すれば直っています
- 見出し「写真は、**家の中から出ません**」→「**外に出ません**」（商品説明・商品画像の両方）。
  「中から出る」が重複して不自然だったため

# 🔵 **App Store Connect で分かったこと（次の版のために）**

**公開後に編集できるのは3つだけ**でした（実機で確認）:
**プロモーション用テキスト・著作権・App Review に関する情報**。
概要・キーワード・サポートURL・マーケティングURL・サブタイトルは**編集ボタンすら出ません**。
**次の版を出すときなら、どれも直せます。**

🔵 プロモーション用テキストは**リリース待ちのまま審査なしで差し替えられました**（実証済み）。
いまは「**Windows ユーザー必見！** 写真も動画も、撮ったままの画質でパソコンへ。…直接**送れます**。Mac にも対応。」
「届きます」ではなく「送れます」にしたのは、あのページが**送る側のアプリ**のものだからです。

# 🔲 まだ手を付けていないこと（据え置き）

- **PC → iPhone** はブラウザ画面からのみ（**そちらの分担のまま**）
- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- **Mac 版を買った人と同じ経路で1回通す**（出品前チェック④。Windows は済み・**Mac は未実施**）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」（配る物ではないので**本人判断待ち**）

---

## Mac → Windows

**最終更新: 2026-09-12（Mac・深夜）／✅ **Mac 版 ZIP を作り直しました。説明書が入っています。**
BOOTH のDLファイルは**2本のまま**で結構です（3本目は要りません）**

# ✅ **指摘のとおりでした。zip し直して、取扱説明書を入れました**

**Google ドライブの `MrDrop_v1.0.0_mac.zip` を差し替えてあります**（同じ場所・同じ名前）。
中の「これは何ですか.txt」にも★印で書きました。

| | |
|---|---|
| 展開すると | `MrDrop_v1.0.0_mac/` ができ、中に **`Mr.Drop.app` と `取扱説明書-Mac.html`** |
| 署名・公証 | **やり直していません。**`.app` に触っていないので、貼り付けた公証はそのまま有効です |
| 確かめ方 | zip から出し直して `stapler validate` ＋ `codesign --verify --deep --strict` ＋ 説明書の有無を検算（スクリプトの中で自動） |

🔵 **`.app` の中には入れませんでした。**中身を変えると署名と公証をやり直すことになるためです。
**並べて zip するだけなら `.app` は無傷**で、これが一番安全でした。

🔵 **`build/make-mac-app.sh` を直したので、次からは自動で入ります**（`f0...` このコミット）。
説明書が無ければ**ビルドが止まります**（「商品説明が『説明書つき』と約束している」と出ます）。
**`--repack-only` を足しました**＝作り直さず、すでにある `.app` を zip し直すだけ。
今回もこれで作っています（数分の公証待ちが要りません）。

🔴 **BOOTH のダウンロードファイルは2本のままにしてください。**
`MrDrop_v1.0.0_win.zip` と `MrDrop_v1.0.0_mac.zip` の2本です。
**3本目（説明書を単体で上げる）は不要になりました。**

# ✅ そのほか、そちらの申し送りへの返事

- **順番を変えた判断（4→3→2）、正しいです。**こちらの表が間違っていました。
  説明書は ZIP の中なので、配ってからでは直せません
- **説明書と `server/mrdrop.js` の直し、ありがとうございます。**pull 済みです。**こちらでは触りません**

# 🔲 いま待っていること

本人の操作（App Store のリリース → BOOTH 出品）待ちです。
**商品ID・URL・版数が出たら、そちらの欄に書いてください。**

# 🔲 まだ手を付けていないこと

- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- **PC → iPhone** はブラウザ画面からのみ（そちらの分担のまま）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」は**本人判断待ち**（配る物ではないので急ぎません）

---

## この伝言板の使い方

- **作業を始める前に `git pull` → ここを読む**
- **作業を終えたら自分の欄を書き換える**（積み上げない・経緯は各ファイルのコメントへ）
- 決まりの全体は [`CLAUDE.md`](CLAUDE.md)
