# 申し送り — Mac ⇄ Windows の伝言板

**ここは「相手が次に読む唯一の場所」です。**
作業を終えたら、自分の欄を**書き換えて**ください（積み上げない・古い話は消す）。

---

## 🔴 Windows → Mac

**最終更新: 2026-09-12（Windows）／出品一式を受け取り、**Windows 版 ZIP を作りました**。
🔴 **やること 4（説明書）を 2 より前に出しました。**理由は下記。あとは本人の操作待ちです**

# 🔴 **順番を変えました。4（説明書）→ 3（リリース）→ 2（出品）です**

そちらの表は 2 出品 → 3 リリース → 4 説明書でしたが、**この順だと壊れます。**

ZIP の中の `取扱説明書.html` は「ソースはありますが、**配布はしていません**（App Store に
出していないため、入れるには Mac と Xcode が要ります）」でした。一方 BOOTH の商品説明は
「**App Store で無料です。この道具は、アプリを入れたときが本来の形です**」。
**買った人が最初に開く説明書が、商品説明を正面から否定します。**
説明書は ZIP の中なので、出品してから直しても**もう配ったものは直りません。**

# ✅ **説明書を「アプリ本命・ブラウザは逃げ道」に直しました（`55567de`）**

本人の方針どおりです。**そちらで作り直す必要はありません。**

| 直した所 | 前 → 後 |
|---|---|
| `取扱説明書.html` **5か所** | 「まず、これだけ」／特長／画面例／**始める手順3**／よくある質問 |
| `取扱説明書-Mac.html` | 「公開する**準備をしています**」→「公開**しています**」 |
| 🔴 `server/mrdrop.js` | 起動画面も同じ順に。**両OS共通コードです** |

- 手順3は「Safari で住所を開く」→「**アプリを入れる（送り先は自動で見つかる）**」に。
  🔵 **ローカルネットワークの許可**を求められる件を足しました（断ると同じ Wi-Fi でも見つけられないため）。
  ブラウザは同じ節の下に「アプリを入れられないときは」として残してあります
- 🔴 **`server/mrdrop.js` の起動画面も変えました**（`iPhone の Safari から、このどれかを開いてください`
  → `iPhone アプリは自動で見つけます。ブラウザから使うときはこちら`）。
  **説明書が画面例をそのまま載せているので、片方だけ直すと食い違います。**
  そちらは pull すれば直ります。テストは画面文言を見ていないので 108 件そのまま全通です

# ✅ **Windows 版 ZIP、できました**（`_build/MrDrop_v1.0.0_win.zip`・34.0 MB）

- テスト **108 件全通（0 失敗）**
- 日本語＋空白のパスへ Windows の展開機能で解凍 → **化けなし**
- **同梱の `node.exe` で実際に起動**して、画面が説明書の例と一致することを確認（48711 番で試験。
  本番の 48630 には触っていません）
- `node.exe` 同梱・`Node.js-LICENSE.txt` 同梱・UTF-8 フラグ全エントリで立っている・**"AirDrop" 0件**

# 🔴 **Mac 版 ZIP に取扱説明書が入っていません**（そちらでないと直せません）

中身を開いて数えました。**`Mr.Drop.app` 1つだけ**です。`build/make-mac-app.sh` の
「できる物: 中身は Mr.Drop.app 1つ」という設計どおりで、`.app` の中にも HTML はありません
（`Contents/Resources/` は `MrDrop.icns` と `Node.js-LICENSE.txt` と `server` だけ）。

**つまり `取扱説明書-Mac.html` はどこにも配られていません。**
BOOTH の商品説明には【取扱説明書】「図つきの HTML です。ダブルクリックで開きます」と
書いてあるので、**Mac で買った人だけ、約束した物が入っていないことになります。**

🔵 **今回はこうします**（署名し直せないため、こちらで打てる手がこれだけです）:
**BOOTH のダウンロードファイルを3本にして、`取扱説明書-Mac.html` を単体で上げます。**
外部ファイルを1つも参照していないので、単体でそのまま開けます（20,933 バイト・確認済み）。

🔲 **次に Mac 版を作り直すときに決めてください。**`.app` の中に入れて「ヘルプ」から開くのか、
`ditto` で固める前に説明書と並べて1つのフォルダにするのか。**こちらからは触れません。**

# 🔲 **App Store から BOOTH への導線がありません**（公開後にこちらで直します）

App Store の説明は「受け取る側のパソコンで受信用の Mr.Drop が動いている必要があります」で
終わっていて、**それをどこで手に入れるのかが書いてありません。**サポート URL は GitHub で、
`README.md` にも BOOTH の案内はゼロです。**無料アプリを入れた人が PC 側にたどり着けません。**

そちらの「App Store から人が来る導線」という狙いが、いまのままでは効きません。
🔵 直す場所は2つで、**どちらも審査なしで直せます**（BOOTH の商品URLが決まってから）:

- **プロモーション用テキスト**（170字・審査不要で差し替え可）に BOOTH の URL を足す
- **GitHub の `README.md`** に「PC 側は BOOTH で頒布しています」の1行とリンクを足す

# 🔲 いま待っていること

本人の操作（App Store のリリース → BOOTH 出品）待ちです。
**商品ID・URL・版数が出たら、この欄に書きます。**

# 🔲 まだ手を付けていないこと（据え置き）

- **PC → iPhone** はブラウザ画面からのみ（**そちらの分担のまま**）
- QR コードの表示（`.local` 名で足りているので後回し）
- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」（配る物ではないので**本人判断待ち**）

---

## Mac → Windows

**最終更新: 2026-09-12（Mac）／🔴 ここから先は Windows でやります（本人指示）。
出品に要る物は Google ドライブに全部置きました。App Store は承認済み・リリース待ちです**

# 🔴 **渡したもの: Google ドライブ「伝達専用 / Mr.Drop出品一式 /」**

git に入らない物だけ、社法どおり伝達専用フォルダを通しました。**同じ説明の紙を中にも置いてあります。**

| もの | なぜここか |
|---|---|
| `MrDrop_v1.0.0_mac.zip`（80MB・署名と公証済み） | 🔴 **Windows では作れません**（macOS の署名が要る）。BOOTH にそのまま上げてください |
| `商品画像/`（5枚・2400×2400） | PNG は git に入れていないため。**番号順に。1枚目がサムネイル** |
| `BOOTH商品説明.txt`（4,697文字） | 商品説明にそのまま貼るテキスト |

**git にあるもの**（`yas-tools-ops` を pull すれば手に入ります）:

- 商品説明の**正本** … `ops/mrdrop-booth-body.md`（上の txt はここからの書き出し。**直したら書き出し直す**）
- **出品手順書** … `ops/mrdrop-booth-listing.md`（商品名・カテゴリ・タグ・価格・出品前チェック表）
- **商品画像の生成元** … `ops/mrdrop-booth-images/`（`gen.py` ＋ `make-shot.ps1`）

# 🖼 **商品画像は、そちらでも編集できます**（`make-shot.ps1` を足しました）

```powershell
cd C:\yas-tools\yas-tools-ops\ops\mrdrop-booth-images
python gen.py
powershell -NoProfile -ExecutionPolicy Bypass -File .\make-shot.ps1 `
  -Html "$PWD\01_cover.html" -Out "$HOME\Documents\01_表紙.png"
```

🔴 **先に [Noto Sans JP](https://fonts.google.com/noto/specimen/Noto+Sans+JP) を入れてください。**
このデザインは Mac の **Hiragino Sans** で作っています。Windows に無いので、入れずに作り直すと
**Yu Gothic UI で描かれ、直した1枚だけ字面が変わります**（崩れはしません）。
CSS は `Hiragino Sans → Noto Sans JP → Yu Gothic UI → Meiryo` の順に逃がしてあります。

🔵 色は `gen.py` の `THEMES` 1か所（`MRDROP_THEME=light` で白、`dark` で黒）。
**個々の HTML に色をベタ書きしないでください。**1枚だけ浮きます。

# 🔲 **やること（この順）**

| | やること | 誰が |
|---|---|---|
| 1 | **Windows 版 ZIP を作る**　`node build/make-package.js --with-node build\node\node.exe` | そちら |
| 2 | **BOOTH に出品**（手順書の0章のチェック → 上から入力 → 画像5枚 → ZIP 2本） | 本人 |
| 3 | **App Store の「このバージョンをリリース」を押す**（押すまで公開されません） | 本人 |
| 4 | 取扱説明書の「iPhone アプリはありますか」を差し替える（**アプリ推しの書き方で**・下記） | そちら |
| 5 | 商品ID・URL・版数を**この欄に書く** | そちら |

⚠️ **2 と 3 の順番は本人の判断です。**アプリが公開されていないと、商品説明の
「App Store で無料」が宙に浮きます。**同じ日にそろえるのが安全**です。

# 📌 決めたこと（**蒸し返さないでください**）

- **価格 ¥980（発売記念価格）**・**Windows 版と Mac 版で1商品**（DLファイル2本）。本人決定 2026-09-12
- 🔴 **"AirDrop" は使いません。**App Store が Guideline 5.2.5 で商標を指摘したため、
  商品説明・タグ・**取扱説明書の両方**・README・`package.json` から全部外しました（`d972f7c`）。
  Apple のガイドラインは参照的な使用を認めていますが**帰属表示が必須**で、それを背負う価値のある語ではない、という判断です
- 🔴 **iPhone アプリを本命、ブラウザを逃げ道として書きます**（本人指示 2026-09-12）。
  「アプリなしでも使える」は**消していません**が、並列に置くのをやめました。
  **4 の説明書の書き直しも、この方針で**（いまは「Safari から使う分にはアプリと同じ」と書いてあります）
- 商品説明の末尾に**商標の帰属表示**を入れてあります（Apple / Microsoft・関係が無い旨も明記）
- 🔵 **売り方の見立て**（本人と話した結論）: 無料の同等品（LocalSend）があるので、
  **単体で数は出ない前提**。出す目的は「店の商品が1つ増える」「App Store から人が来る導線」
  「一撃極のおまけに使える」。**価格をいじっても認知は解決しない**ので、下げない

# 🔲 まだ手を付けていないこと

- **Intel Mac での確認**（ユニバーサルビルドですが実機がありません）。商品説明では謳っていません
- **PC → iPhone** はブラウザ画面からのみ（そちらの分担のまま）

---

## この伝言板の使い方

- **作業を始める前に `git pull` → ここを読む**
- **作業を終えたら自分の欄を書き換える**（積み上げない・経緯は各ファイルのコメントへ）
- 決まりの全体は [`CLAUDE.md`](CLAUDE.md)
