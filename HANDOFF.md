# 申し送り — Mac ⇄ Windows の伝言板

**ここは「相手が次に読む唯一の場所」です。**
作業を終えたら、自分の欄を**書き換えて**ください（積み上げない・古い話は消す）。

---

## 🔴 Windows → Mac

**最終更新: 2026-09-12（Windows）／🎉 **発売しました。** App Store 05:47 公開・BOOTH 05:51 公開。
そちらの宿題は**ありません**。下は「知らないと戻してしまう」ものだけです**

# 🔴🔴 **最優先のお願い：アプリを入れた人が、次に何をすればいいか分かりません**

**本人が気づきました（2026-09-12）。規約の話ではなく、商品の穴です。**

`ios/MrDrop/ContentView.swift:173` の「見つからないときは」の案内は、こうなっています:

> ・受け取る側のパソコンで **Mr.Drop（PC 版）** が動いている必要があります

**「要る」とは書いてあるのに、それをどこで手に入れるのかが、どこにも書いてありません。**
App Store の説明文も「受信用の Mr.Drop が動いている必要があります」で終わっていて、入手先ゼロ。
アプリの中にも外部リンクが1つもありません（Swift を全部見ました。出てくる URL は接続先の PC だけ）。

🔴 **入れた人は、そこで詰まります。**無料アプリを入れて何もできず、どうすればいいかも分からない。
**レビューがゼロの新作にとって、★1 が一番こわい**です。

🔲 **`ios/` は Xcode が要るのでそちらの領分です。次の版で直してください。**
**入手先の1行を足すだけ**です。例:

> ・受け取る側のパソコンで **Mr.Drop（PC 版）** が動いている必要があります
> 　**PC 版は yas-tools.booth.pm で頒布しています**

🔵 **リンクボタンにせず、文字だけ置く**のが落としどころだと考えています。
`https://` のボタンを置くと「購入への導線」の形になりますが、**文字で書くだけなら案内**です。
⚠️ **3.1.1 に触れないかは一次情報で裏取りできていません。**
2026-09-03 に「アプリの中に『PC 版を買う』導線は置かない」と決めていますが、
**あの決定は、この穴に気づく前のもの**です。判断は本人と相談してください。

🔵 **新しい版を出すときは、ついでに「マーケティングURL」に BOOTH を入れられます**
（公開済みの版では編集できませんが、新しい版なら入ります）。

## ✅ こちらで先に手を打ったこと（2026-09-12）

**いま直せる場所が1つだけあったので、そこを使いました。**
App Store の**プロモーション用テキスト**（説明のいちばん上・**審査不要**）を、
売り文句から**「先に何が要るか」の案内**に差し替えました:

> 【先にご確認ください】これは「送る側」のアプリです。受け取るパソコン（Windows / Mac）側にも
> Mr.Drop を入れて動かしておく必要があります。両方が同じ Wi-Fi につながっていれば、
> 写真も動画も撮ったままの画質で直接送れます。

**これで「入れる前」には気づけます。**ただし**「入れたあと」に困っている人には届きません。**
そこがアプリ側の宿題です。

🔵 `README.md` も「手に入れる」（売る書き方）から
**「2つそろって、はじめて動きます」**へ書き直しました（`0f178c0`）。

---

# 🎉 **発売しました**

| | |
|---|---|
| **iPhone アプリ** | <https://apps.apple.com/jp/app/id6808082392>（無料・1.0.0 (1)・05:47 公開） |
| **BOOTH（PC側）** | <https://yas-tools.booth.pm/items/8832410>（**¥500**・商品ID 8832410・05:51 公開） |

🔴 **順番は「App Store のリリース → BOOTH 公開」でやりました。**商品説明が「App Store で無料」と
書き、末尾に実URLを貼っているので、逆にすると買った人が 404 に飛びます。
🔵 **リリースを押してから Apple 側の反映まで約4分**でした（404 → 200）。次も待つこと。

# 🔴 **戻さないでほしいもの（3つ）**

**そちらの手順書・README と食い違っているので、知らないと元に戻します。**

| 直した所 | そちらの記述 | いまどうなっているか |
|---|---|---|
| `gen.py` の `.term` の**色** | README「**どのテーマでも黒のまま**（実物の画面がそうなので）」 | 🔴 **白にしました**（本人指示）。理由は CSS にコメントで残してあります |
| `gen.py` の `.term` の**フォント** | README「Noto Sans JP を入れれば Mac とほぼ同じ見た目」 | `"SF Mono","Menlo"` は**どちらも Mac 専用**でした。`"Cascadia Mono","Consolas"` を足しています |
| 商品画像の**作り直し手順** | README「`python3` と Chrome があれば動きます」 | 🔴 **Windows に Python が入っていませんでした。**入れて解決（3.13）。**手順書に1行足してください** |

🔵 **フォントを足すまで、Windows で作り直すと別物になっていました**（実測）:
バックスラッシュが `¥` で描かれて `C:¥Users¥…` になり、`192. 168. 10. 105` のように字間が開く。
**SF Mono を先頭に残したので、そちらの見た目は変わりません。**

# 🔴 **Mac 版 ZIP のファイル名に UTF-8 の印（bit 11）が立っていません**

差し替えてくださった `MrDrop_v1.0.0_mac.zip` を数えました。**全 54 エントリで 0 件**です。
`ditto` で固めているためで、**Windows で展開すると `取扱説明書-Mac.html` が
`蜿匁桶隱ｬ譏取嶌-Mac.html` に化けます**（実際に化けました）。

🔵 **Mac で展開する分には化けません**（macOS は印が無くても UTF-8 として読む）。買うのは Mac の人なので
**今回はこのまま出しました**。ただ、このリポジトリが `build/make-package.js` でわざわざ避けている罠と
同じものです。🔲 次に作り直すときの扱いは、そちらで決めてください。

# 📌 決まったこと

- **価格 ¥500**（発売記念価格・期限は決めない）。この日のうちに ¥980 →「一週間無料」→
  「一週間だけ ¥500」と動き、最後にここへ落ち着きました。**期限を書かないので、
  「元値・次の価格・期限を書かない」という決めごとはそのまま有効です**
- **取扱説明書と商品説明を「アプリ本命・ブラウザは逃げ道」に統一**（`55567de`）。
  🔴 **両OS共通の `server/mrdrop.js` の起動画面も変えています**。pull すれば直っています
- 見出し「写真は、**家の中から出ません**」→「**外に出ません**」（商品説明・商品画像の両方）。
  「中から出る」が重複して不自然だったため

# 🔵 **App Store には BOOTH の URL を入れていません**（判断の記録）

**マーケティングURL は公開後は編集できません。**編集ボタンが出るのは
**プロモーション用テキスト・著作権・App Review 情報の3つだけ**でした（実機で確認）。

プロモーション用テキストに購入リンクを置く案は**見送りました**。3.1.1 の近くにあるうえ、
**審査を通らない枠なので、間違っていても誰にも止められない**ためです。
代わりに **GitHub の `README.md`**（App Store のサポートURLが指す先）に案内を置きました（`faeec46`）。

🔲 **次のバージョンを出すときに、マーケティングURL へ正攻法で入れてください。**

🔵 プロモーション用テキストは**リリース待ちのまま審査なしで差し替えられました**（実証済み）。
いまは「**Windows ユーザー必見！** 写真も動画も、撮ったままの画質でパソコンへ。…直接**送れます**。Mac にも対応。」
「届きます」ではなく「送れます」にしたのは、あのページが**送る側のアプリ**のものだからです。

# 🔲 まだ手を付けていないこと（据え置き）

- **PC → iPhone** はブラウザ画面からのみ（**そちらの分担のまま**）
- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- **Mac 版を買った人と同じ経路で1回通す**（出品前チェック④。Windows は済み・**Mac は未実施**）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」（配る物ではないので**本人判断待ち**）

---

## Mac → Windows

**最終更新: 2026-09-12（Mac・深夜）／✅ **Mac 版 ZIP を作り直しました。説明書が入っています。**
BOOTH のDLファイルは**2本のまま**で結構です（3本目は要りません）**

# ✅ **指摘のとおりでした。zip し直して、取扱説明書を入れました**

**Google ドライブの `MrDrop_v1.0.0_mac.zip` を差し替えてあります**（同じ場所・同じ名前）。
中の「これは何ですか.txt」にも★印で書きました。

| | |
|---|---|
| 展開すると | `MrDrop_v1.0.0_mac/` ができ、中に **`Mr.Drop.app` と `取扱説明書-Mac.html`** |
| 署名・公証 | **やり直していません。**`.app` に触っていないので、貼り付けた公証はそのまま有効です |
| 確かめ方 | zip から出し直して `stapler validate` ＋ `codesign --verify --deep --strict` ＋ 説明書の有無を検算（スクリプトの中で自動） |

🔵 **`.app` の中には入れませんでした。**中身を変えると署名と公証をやり直すことになるためです。
**並べて zip するだけなら `.app` は無傷**で、これが一番安全でした。

🔵 **`build/make-mac-app.sh` を直したので、次からは自動で入ります**（`f0...` このコミット）。
説明書が無ければ**ビルドが止まります**（「商品説明が『説明書つき』と約束している」と出ます）。
**`--repack-only` を足しました**＝作り直さず、すでにある `.app` を zip し直すだけ。
今回もこれで作っています（数分の公証待ちが要りません）。

🔴 **BOOTH のダウンロードファイルは2本のままにしてください。**
`MrDrop_v1.0.0_win.zip` と `MrDrop_v1.0.0_mac.zip` の2本です。
**3本目（説明書を単体で上げる）は不要になりました。**

# ✅ そのほか、そちらの申し送りへの返事

- **順番を変えた判断（4→3→2）、正しいです。**こちらの表が間違っていました。
  説明書は ZIP の中なので、配ってからでは直せません
- **説明書と `server/mrdrop.js` の直し、ありがとうございます。**pull 済みです。**こちらでは触りません**
- **App Store → BOOTH の導線**：そちらの案（プロモーション用テキスト＋README）でお願いします。
  🔵 プロモーション用テキストは**審査なしで差し替えられる**ので、URL が出てからで間に合います

# 🔲 いま待っていること

本人の操作（App Store のリリース → BOOTH 出品）待ちです。
**商品ID・URL・版数が出たら、そちらの欄に書いてください。**

# 🔲 まだ手を付けていないこと

- **Intel Mac での確認**（実機なし。商品説明でも謳っていません）
- **PC → iPhone** はブラウザ画面からのみ（そちらの分担のまま）
- 🔵 `CLAUDE.md` 7行目の「AirDrop の代わり」は**本人判断待ち**（配る物ではないので急ぎません）

---

## この伝言板の使い方

- **作業を始める前に `git pull` → ここを読む**
- **作業を終えたら自分の欄を書き換える**（積み上げない・経緯は各ファイルのコメントへ）
- 決まりの全体は [`CLAUDE.md`](CLAUDE.md)
